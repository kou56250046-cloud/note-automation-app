# 生成ログ — 2026-08-31-schedule-intake

日付: 2026-08-29（生成日）/ 公開予定 2026-08-31
種別: 無料記事
連載: 2026-W36-schedule-heatmap（1つの自動化システムを7分割した月曜・設計）
タイトル: 参加者の空き状況を自由記述で集める。日程調整フォームの入口設計
文字数: 2797 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: 参加者の空き状況を自由記述で集める。日程調整フォームの入口設計（themes.md の id: 2026-W36-01）

スコア: strength 5 / market 4 / willingness 2 / artifact 5 = 16

選定理由:
strength5: account.md のペルソナ「GASで自動化を組んだが人に任せられない人」の入口そのもの。
「◯曜午前は無理、火水木ならいつでも」という自由記述の都合を、Geminiに渡しやすい形で
シートに集める設計は手元の材料だけで書ける。W35B-01（フォーム→シート入口）と同じ骨格だが、
対象データが「アンケートの感想」ではなく「日程の都合」であり、シート構造（回答者名列・
自由記述列・処理状態列に加え、今回は締切日時の管理が必須になる点）が異なる。
market4: market/2026-08-29.md の「GAS」は需要227.5 / 供給23.5件/日で比9.683（有料率11%、
総12,422件）。「GoogleAppsScript」は需要92.5 / 供給10.4件/日で比8.879（有料率13%、総5,374件）。
どちらも計測17タグ中の上位帯。今週この帯を複数回使うため5ではなく4とした。
willingness2: 無料記事想定。設計図だけでは行動が完結せず、次の記事（中核処理）が要る前提。
artifact5: onFormSubmit installable triggerの設定手順＋シート構造の設計図（回答者名/
自由記述/締切日時/処理状態列）＋受信〜シート書き込みのGASコード全文を出せる。
次点: 「経費申請の受付フォーム設計」は同じ骨格で検討したが、金額・経費という題材は
ethics-line観点でリスクが増えるため、この系統自体を選ばなかった（下の除外理由を参照）。

今日これを選んだ理由: weekly-research が day: mon を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-29（土曜）に行った。market データは research/market/2026-08-29.md を当日取得
しているため、判断材料は最新である。**むしろ規定より新しい。**

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告9 | — |
| dedup.py | 最大類似度 0.351（notes/2026-08-24-form-intake/02-final.md） | — |
| reader-feedback | 実行した（週1本・月曜の記事） | — |
| ethics-line（light） | 起動した（clear: true / round 1） | — |

### reader-feedback

`critic` サブエージェントで1周だけ実行した（週1本・月曜の記事に掛ける規定）。

判定: **不合格（pass: false / round 1 of 1）** → `daily-article` へ差し戻し、修正済み。

| ID | 判定 | 内容 |
|---|---|---|
| F1 冒頭3行 | **× drop** | L1・3・5・19・21。読者が読んでいない別記事（前週のアンケート要約）との差分から始まっており、「アンケートの感想」が何を指すか分からない。19行目の見出し「アンケートとは違うシートを作る理由」と21行目「前回書いたアンケート要約の入口設計と、骨格はよく似ています」で、記事の骨格自体が前回参照だと確定する。読者は「連載の途中で自分は対象外」と判断して閉じる |
| F2 結論の位置 | ○ | — |
| F3 情報の新しさ | ○ | — |
| F4 具体性 | ○ | — |
| F5 長さと密度 | **× note** | L81 と L186 で、回答者名の表記揺れを自動吸収しないという主張が重複していた（186行目自身が「前述のとおり」と書いていた） |
| F6 出口 | ○ | — |

**修正した内容（全件）**

- F1: 1〜5行目からアンケートへの言及を削除し、読者の動作（「火水木ならいつでも」という自由記述を手で表に書き写している）から始める形に置き換えた。19行目の見出しを「日程調整のシートに必要な3つの条件」に変更し、21行目の前回参照の1文を削除して「フォームの回答をインストーラブルトリガーで受け取り、シートに1行ずつ積む。ここまでは、どんな用途でも同じ形です」に差し替えた
- F5: 81行目を「回答者名は完全一致で突合します」の1文に削り、表記揺れの説明と運用側の対策を186行目の「つまずきどころ」に一本化した

**この指摘は連載の構造的な弱点を突いている。** CLAUDE.md は「各記事は単体で完結させる。『前回の続き』で始めない」と定めているが、
連載の月曜（設計）は前週の月曜と役割が同じであるため、書き手が前週との差分で説明しようとする力が働く。
`weekly-build` が `writer` に渡す指示に「既出記事と重ならないように」と書いていることが、
かえって「既出記事との違い」を本文の骨格にさせた可能性がある。下の申し送りに書いた。

### ethics-line — 削除・修正した内容（全件）

**起動した。** `lint.py` の `needsAudit` が `true` だったため（CLAUDE.md ルール6）。

`auditor` による自動修正は 0 件（severity: block の検出が無かった）。


**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

`auditor` が人間の判断に残した項目が 2 件ある。

- L[44] [E3]「共有アカウントで作業していると、ダイアログが出ずに止まって見えることがあります。」
  - GAS の権限承認ダイアログが出ない、という具体的な挙動の主張。仕様の説明とも、実際に踏んだ観測の主張とも読める。profile.md の「体験」は空であり、この挙動を検証した記録は無い。ただし嘘だと断定できない
  - 対応: 自動修正しない。本人に GAS 上で確認した根拠があるかを確認する。無ければ「所有者本人のアカウントで承認してください」という指示だけを残し、挙動の描写を落とす
- L[77] [E3]「日程調整では、参加者が予定を変えて2回目の回答を送ってくることがよくあります。」
  - 「よくあります」は母数のない頻度の一般化。ethics-line の E3 は母数のない一般化を対象に含む。ただし自分の実績・自システムの効果の主張ではなく、領域一般の常識の範囲にとどまるため嘘だと断定できない
  - 対応: 自動修正しない。気になる場合は「送ってくることがあります」に弱めるだけで足りる


---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 収益額・作業時間の削減率・利用者数。いずれも `profile.md` の「書けないこと」に該当する
- **このシステムを実際に運用した結果。** 書き手は日程調整ヒートマップを運用しておらず、
  `profile.md` の「体験」セクションは空である。検証した観測として書ける事実が無いため、
  挙動はすべて仕様の説明として書いた
- `profile.md` の実績（記事45本 / フォロワー101人 / スキ中央値6.5）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | `00-theme.md` の sources で足りた。追加の web 検索は行っていない |
| `weekly-build` | `writer` への指示に「既出記事と重ならないように」と書いた結果、既出記事との差分が本文の骨格になった（reader-feedback の F1）。**「重ならないように」ではなく「既出記事に言及せずに書く」と指示すべきである。** 連載の月曜は前週の月曜と役割が同じなので、ここが毎週起きる |
| `account-design` | サブカテゴリ③の既定タグは `NotebookLM` 固定だが、今週も材料が Gemini API 側だけであり在庫側で `GeminiAPI` に差し替えている。2週続けて同じ差し替えが発生した。account.md のハッシュタグ表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告9件・ブロックなし）

- L11 [sentence-length] 一文が 63 文字（目安 60）
- L19 [heading-interval] 前の見出しから 199 文字（目安 300）。見出しが多すぎる
- L31 [heading-interval] 前の見出しから 212 文字（目安 300）。見出しが多すぎる
- L33 [sentence-length] 一文が 62 文字（目安 60）
- L77 [unverified-claim] 人数の主張。profile.md に根拠が無ければ書けない（ethics-line を起動する）
- L83 [heading-interval] 前の見出しから 160 文字（目安 300）。見出しが多すぎる
- L182 [heading-interval] 前の見出しから 91 文字（目安 300）。見出しが多すぎる
- L188 [heading-interval] 前の見出しから 243 文字（目安 300）。見出しが多すぎる
- L196 [sentence-length] 一文が 62 文字（目安 60）

### dedup.py

- 既出記事との最大類似度 **0.351**（比較相手: notes/2026-08-24-form-intake/02-final.md）
- 週内の他記事との最大類似度 **0.247**（比較相手: 2026-09-04-slot-notify）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが高くなることがある。
実測では 0.15〜0.25 に収まり、警告閾値 0.70 を大きく下回った。

**前週（2026-W35B・アンケート自由記述の要約）との比較が最も高い。** 同じ役割の回どうしは
骨格が似るためである。それでも 0.17〜0.35 にとどまったのは、中核処理の変換型を
「要約・分類」から「抽出・正規化」に変えたことが効いている（weekly-research の系統選定）。
