# レター診断 — 2026-08-20-design-tokens / round 3

型: tool（道具型）
判定: **合格（16/16）**

round 2 で `upstreamSuspect: product-concept` を指し、`00-concept.md` の価値軸を
変更したうえでの再判定である（`letter-meta.json` の `upstreamRevision`）。

---

## × 残っている項目

なし。

---

## ○ 通過した項目

A1 A2 A3 A4 B1 B2 B3 C1 C2 C3 C4 C5 C6 D1 D2 D3

---

## A1 の判定根拠（2周連続で×だった項目のため記録する）

### 1. 交互配置

理想の未来と再現性の隣接なし。

```
hook(1-3) → problem(5-9) → IF(11-13) → R:単純化(15-17) → IF(19-21)
→ R:簡易化(23-25) → fear(27-29) → IF(31-33) → R:開示+動作条件(35-94)
→ IF(96-100) → R:不便だった状況(102-106) → objection(108-126)
→ funnel(128-130) → IF:締め(132-134)
```

同種2連続は0箇所。

### 2. 着地の文型

- 13行「`tokens.css` の中に残っています。開けば、そこに書いてあります」— 肯定
- 21行「AIへの指示も、他人へのレビュー依頼も、同じ言葉で通ります」— 肯定
- 33行「持っていくものは、それだけで足ります」— 肯定
- 100行「やることリストから1つ消えます」— 否定
- 134行「相手が見るのは、AIが選んだ色ではなく、あなたが決めた色です」— 肯定

肯定4・否定1。round 2 の「6箇所すべてが否定形」は解消。

### 3. 未来の実質が分かれているか

各ブロックが前のブロックに無かった絵を1つ足している。

- 11-13行 — 前回の指示が**ファイルに残っている**（記録）
- 19-21行 — 「なんか違う」ではなく**名前で指せる。他人へのレビュー依頼にも同じ語が通る**（語彙／他者が初出）
- 31-33行 — **案件が変わっても手元は1枚**（移植）
- 96-100行 — ダーク対応が**リストから消える**（工程）
- 132-134行 — **スクリーンショットをそのまま人に送れる**（他者の目）

round 2 の×は「5軸が名前だけ違って『書き手の作業が減る』1本に帰着している」だった。
132-134行と19-21行は減算に帰着しない（前者は他者が見るものの話、後者は指示の解像度）。
1つの便益への収束は成立していない。

### 4. `account.md` 112行の欲求が未来として描かれているか

`account.md` 116行「動くものは作れるのに、スクリーンショットを人に見せる気になれない」に対し、
134行が「そのまま撮ったスクリーンショットを、人に送れます」で直接反転している。
「毎回」は132行「次にAIへ『作り直して』と頼んだとき、出てくるのは前と同じ色の画面です」が担う。
112行の欲求が未来として描かれている。

### 残る指摘（×の根拠にはしない）

11行「同じ色から始まります」／31行「前と同じ見た目が立ち上がっています」／
132行「前と同じ色の画面です」は、導入部の命題が3回重なる。
ただし各ブロックの着地（13・33・134行）が別の絵に分かれており、
また132行は締めの導入として必要な文であるため、削除を指示できる修正案が立たない。
中間評価を作らないため○とする。

---

## 型による分岐項目の確認

**A3（`tool` の4点）**

- 単純化 — 15-17行「やることは1つだけです」「`tokens.css` を直下に置くこと」
- 簡易化 — 23-25行「1枚」「`node check-tokens.mjs .` の1回だけ」「Node 18」
- 成果物の開示 — 39-80行に色トークン全文、90-94行に検出ログ実例3行
- 不便だった状況 — 102-106行「ファイルをさかのぼって探す」「似た青が3つ、4つと増えていく」

`type: tool` のため「天井を下げる」「共感」は探していない。

**C2（`tool` の3点）**

- 中身の開示 — 39-80行・90-94行（`toolEvidence` の記録行を実読して確認）
- 最初の1歩 — 17行・112行
- 動作条件 — 84-86行（Node 18 / CSSカスタムプロパティ / Claude Code・Gemini・ChatGPT）

`type: tool` のため「自分以外の結果がない」ことを×の理由にしていない。

---

## 争点になりうる項目の判定根拠

**C3** — 114行の値上げ告知は `pricing.json` の `priceSchedule`
（1,480円 / 〜2026-09-30 → 1,980円 / 2026-10-01〜）と一致。`deadlineIsReal: true`。

**C4** — 114-116行は値引きの構文だが、C3 が要件として求める値上げ告知そのものであり、
これを C4 の×根拠にはできない。中身の分解は120行（5系統・検査スクリプト・渡し方2種・作例3本）に
独立して存在し、「配色を1回作り直す手間より、たいてい安く済みます」で納得を作っている。

**D1** — 「〜かもしれません」「人によります」は0箇所。
102行「がち」・120行「たいてい」はヘッジだが、読者に判断を投げ返していない。
中核主張（19・21・25・124・126・132行）はすべて断定形。

**B1** — 恐怖ブロックは27-29行のみ。102-106行は A3 の「不便だった状況」であり、
現状描写かつ内容も27-29行と同一の問題圏（作り直しの反復）にある。地獄は1つ。

---

## 判定の履歴

| round | score | pass | failures | 何をしたか |
|---|---|---|---|---|
| 1 | 14/16 | false | A1・C3 | A1: 再現性が2連続 → 96-100行に理想の未来を挿入。C3: 期限が不在 → `pricing-strategy` に戻り `priceSchedule` を追加 |
| 2 | 15/16 | false | A1 | 交互配置は解消。理想の未来4か所がすべて否定形に収束し、価値軸5本が1つの便益に帰着していた。`upstreamSuspect: product-concept` で打ち切り |
| 3 | **16/16** | **true** | — | `00-concept.md` に価値軸6「見え方（他者の目）」を追加。13・33・134行を肯定形に変更、締めを軸6に差し替え |

round 2 の判定詳細は `letter-meta.json` の `auditHistory` に残している。

---

## 上流の見直し

不要。round 2 で指した `product-concept` の修正で A1 が解消した。
`00-concept.md` に価値軸6を立てたことが直接の原因である。

`returnTo: null`。次工程（`ethics-line`）へ進んでよい。

**`ethics-line` に必ず回すこと。** 114行の値上げは `deadlineClaims` に
`isReal: true` として記録されているが、**2026-10-01 に人間が note の価格設定画面で
1,980円に変更しなければ、景品表示法の有利誤認に該当する。** 実行責任は人間側にある。
