# 生成ログ — 2026-09-02-lock-schema

日付: 2026-08-29（生成日）/ 公開予定 2026-09-02
種別: 無料記事
連載: 2026-W36-schedule-heatmap（1つの自動化システムを7分割した水曜・失敗）
タイトル: 同時送信の二重書き込みとGeminiの形式崩れを防ぐ。壊れる集計コードと直したコード
文字数: 2819 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: 同時送信の二重書き込みとGeminiの形式崩れを防ぐ。壊れる集計コードと直したコード（themes.md の id: 2026-W36-03）

スコア: strength 5 / market 4 / willingness 2 / artifact 5 = 16

選定理由:
strength5: learnings.mdに「連載の水（失敗時の扱い）の材料として再利用可」と明記されている
LockServiceの排他制御と、Geminiのレスポンス検証の材料をそのまま使えるが、対象を
「同時にフォームが2件送信されてシートの同じ行を2回処理する」ケースに置き換えている点、
加えてGeminiが期待したJSON形式（曜日×時間帯の配列）で返らなかったときのバリデーションと
再試行という、水曜特有の失敗モードを扱う点でW35B-03とは題材が異なる。
market4: 「GAS」比9.683（需要227.5/供給23.5件/日、有料率11%）、「GoogleAppsScript」比8.879
（需要92.5/供給10.4件/日、有料率13%）。
willingness2: 無料記事想定。トラブル対処は知って終わりに近く、成果物の受け渡しは発生しない。
artifact5: 「同時送信でシートの同じ行を2回処理してしまうコード→LockService.tryLockで
直したコード」と「Geminiのレスポンスが期待したJSON形式で返らず落ちるコード→スキーマ検証＋
最大3回の再試行で直したコード」の2対を出せる。
次点: 「同一人物の再送信を検出して上書きする」処理も検討したが、メールアドレス突合の
仕組みまで含めると水曜1本に収まらないため、今回は同時実行の排他制御とJSON形式の検証に絞った。

今日これを選んだ理由: weekly-research が day: wed を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-29（土曜）に行った。market データは research/market/2026-08-29.md を当日取得
しているため、判断材料は最新である。**むしろ規定より新しい。**

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告13 | — |
| dedup.py | 最大類似度 0.291（notes/2026-08-26-lock-retry/02-final.md） | — |
| reader-feedback | 実行していない（週1本のみ。月曜の記事に掛けた） | — |
| ethics-line（light） | **起動していない**（lint.py の needsAudit が false） | — |

### reader-feedback

- 実行していない


### ethics-line — 削除・修正した内容（全件）

**起動していない。** `lint.py` の `needsAudit` が `false` だったため（CLAUDE.md ルール6）。

`lint.py` は E5（断定表現）を `voice.md` の `ng` で、E3（裏付けのない実績）を
`unverified-claim`（金額・割合・人数・期間・フォロワー数）で一次判定する。
どちらも検出がなかったので `auditor` を呼んでいない。


**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

- なし


---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 収益額・作業時間の削減率・利用者数。いずれも `profile.md` の「書けないこと」に該当する
- **このシステムを実際に運用した結果。** 書き手は日程調整ヒートマップを運用しておらず、
  `profile.md` の「体験」セクションは空である。検証した観測として書ける事実が無いため、
  挙動はすべて仕様の説明として書いた
- `profile.md` の実績（記事45本 / フォロワー101人 / スキ中央値6.5）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | `00-theme.md` の sources で足りた。追加の web 検索は行っていない |
| `account-design` | サブカテゴリ③の既定タグは `NotebookLM` 固定だが、今週も材料が Gemini API 側だけであり在庫側で `GeminiAPI` に差し替えている。2週続けて同じ差し替えが発生した。account.md のハッシュタグ表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告13件・ブロックなし）

- L13 [sentence-length] 一文が 67 文字（目安 60）
- L17 [heading-interval] 前の見出しから 185 文字（目安 300）。見出しが多すぎる
- L84 [sentence-length] 一文が 62 文字（目安 60）
- L92 [sentence-length] 一文が 63 文字（目安 60）
- L200 [sentence-length] 一文が 86 文字（目安 60）
- L204 [sentence-length] 一文が 68 文字（目安 60）
- L212 [sentence-length] 一文が 66 文字（目安 60）
- L218 [heading-interval] 前の見出しから 251 文字（目安 300）。見出しが多すぎる
- L222 [heading-interval] 前の見出しから 77 文字（目安 300）。見出しが多すぎる
- L234 [heading-interval] 前の見出しから 95 文字（目安 300）。見出しが多すぎる
- L238 [heading-interval] 前の見出しから 67 文字（目安 300）。見出しが多すぎる
- L240 [sentence-length] 一文が 82 文字（目安 60）
- L241 [sentence-length] 一文が 81 文字（目安 60）

### dedup.py

- 既出記事との最大類似度 **0.291**（比較相手: notes/2026-08-26-lock-retry/02-final.md）
- 週内の他記事との最大類似度 **0.214**（比較相手: 2026-09-01-slot-extract）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが高くなることがある。
実測では 0.15〜0.25 に収まり、警告閾値 0.70 を大きく下回った。

**前週（2026-W35B・アンケート自由記述の要約）との比較が最も高い。** 同じ役割の回どうしは
骨格が似るためである。それでも 0.17〜0.35 にとどまったのは、中核処理の変換型を
「要約・分類」から「抽出・正規化」に変えたことが効いている（weekly-research の系統選定）。
