# 生成ログ — 2026-09-01-slot-extract

日付: 2026-08-29（生成日）/ 公開予定 2026-09-01
種別: 無料記事
連載: 2026-W36-schedule-heatmap（1つの自動化システムを7分割した火曜・中核）
タイトル: Geminiに自由記述の空き時間を構造化させる。プロンプトと呼び出しコードの全文
文字数: 2642 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: Geminiに自由記述の空き時間を構造化させる。プロンプトと呼び出しコードの全文（themes.md の id: 2026-W36-02）

スコア: strength 5 / market 5 / willingness 2 / artifact 5 = 17

選定理由:
strength5: account.md サブ3「Gemini APIは自動化の材料として扱う」にど真ん中で対応する。
「火水木ならいつでも」のような自由記述を、曜日×時間帯のboolean配列に変換する処理は
要約でも固定ラベルへの分類でもなく、**抽出・正規化**という別の変換タイプであり、
既出の議事録要約（要約）・問い合わせ仕分け（固定ラベル分類）・アンケート要約
（要約＋感情スコア）のいずれとも中核処理の形が異なる。
market5: market/2026-08-29.md の「GeminiAPI」は需要27.0 / 供給1.4件/日で比19.747
（有料率8%、総570件）。計測17タグ中1位。総570件は他タグより母数が小さいニッチ帯である点を
明記する。ハッシュタグにはこちらを主軸に採用した（内容と直接一致するため、既定のNotebookLM
ではなくGeminiAPIを使う。理由はW35B以降と同じ判断基準）。「GAS」比9.683も高い帯。
willingness2: 無料記事想定。プロンプトを知っても、自分のシートに繋ぎ込む工程がまだ残る。
artifact5: 曜日×時間帯のboolean配列（例: {"mon":{"am":false,"pm":true}, ...}）を返す
responseSchemaの定義＋プロンプト全文＋UrlFetchAppでのGemini API呼び出しコード全文を出せる。
次点: 「バグ報告の重複検出＋優先度判定」は中核処理がW35B-02（要約＋カテゴリ＋感情スコアの
JSON化）と構造的に酷似し、分類・スコアリングという同じ変換タイプになるためこの系統は
選ばなかった（下の除外理由を参照）。

今日これを選んだ理由: weekly-research が day: tue を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-29（土曜）に行った。market データは research/market/2026-08-29.md を当日取得
しているため、判断材料は最新である。**むしろ規定より新しい。**

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告8 | — |
| dedup.py | 最大類似度 0.295（notes/2026-08-25-gemini-digest/02-final.md） | — |
| reader-feedback | 実行していない（週1本のみ。月曜の記事に掛けた） | — |
| ethics-line（light） | **起動していない**（lint.py の needsAudit が false） | — |

### reader-feedback

- 実行していない


### ethics-line — 削除・修正した内容（全件）

**起動していない。** `lint.py` の `needsAudit` が `false` だったため（CLAUDE.md ルール6）。

`lint.py` は E5（断定表現）を `voice.md` の `ng` で、E3（裏付けのない実績）を
`unverified-claim`（金額・割合・人数・期間・フォロワー数）で一次判定する。
どちらも検出がなかったので `auditor` を呼んでいない。


**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

- なし


---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 収益額・作業時間の削減率・利用者数。いずれも `profile.md` の「書けないこと」に該当する
- **このシステムを実際に運用した結果。** 書き手は日程調整ヒートマップを運用しておらず、
  `profile.md` の「体験」セクションは空である。検証した観測として書ける事実が無いため、
  挙動はすべて仕様の説明として書いた
- `profile.md` の実績（記事45本 / フォロワー101人 / スキ中央値6.5）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | `00-theme.md` の sources で足りた。追加の web 検索は行っていない |
| `account-design` | サブカテゴリ③の既定タグは `NotebookLM` 固定だが、今週も材料が Gemini API 側だけであり在庫側で `GeminiAPI` に差し替えている。2週続けて同じ差し替えが発生した。account.md のハッシュタグ表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告8件・ブロックなし）

- L45 [sentence-length] 一文が 63 文字（目安 60）
- L87 [heading-interval] 前の見出しから 142 文字（目安 300）。見出しが多すぎる
- L89 [sentence-length] 一文が 71 文字（目安 60）
- L184 [sentence-length] 一文が 63 文字（目安 60）
- L186 [heading-interval] 前の見出しから 249 文字（目安 300）。見出しが多すぎる
- L188 [sentence-length] 一文が 62 文字（目安 60）
- L194 [sentence-length] 一文が 65 文字（目安 60）
- L200 [sentence-length] 一文が 71 文字（目安 60）

### dedup.py

- 既出記事との最大類似度 **0.295**（比較相手: notes/2026-08-25-gemini-digest/02-final.md）
- 週内の他記事との最大類似度 **0.214**（比較相手: 2026-09-02-lock-schema）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが高くなることがある。
実測では 0.15〜0.25 に収まり、警告閾値 0.70 を大きく下回った。

**前週（2026-W35B・アンケート自由記述の要約）との比較が最も高い。** 同じ役割の回どうしは
骨格が似るためである。それでも 0.17〜0.35 にとどまったのは、中核処理の変換型を
「要約・分類」から「抽出・正規化」に変えたことが効いている（weekly-research の系統選定）。
