# 生成ログ — 2026-09-04-slot-notify

日付: 2026-08-29（生成日）/ 公開予定 2026-09-04
種別: 無料記事
連載: 2026-W36-schedule-heatmap（1つの自動化システムを7分割した金曜・運用）
タイトル: 集計のトリガーと権限。全員が空いている枠が決まったらSlackに通知する設定
文字数: 2943 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: 集計のトリガーと権限。全員が空いている枠が決まったらSlackに通知する設定（themes.md の id: 2026-W36-05）

スコア: strength 4 / market 4 / willingness 2 / artifact 5 = 15

選定理由:
strength4: account.md サブ2「権限・スコープ・実行ユーザー。他人に渡すときに必ず詰まる
場所」に対応する。トリガー実行者の権限周りは調べ直しがやや必要なためstrength5ではなく4。
market4: 「GAS」比9.683（需要227.5/供給23.5件/日、有料率11%）、「GoogleAppsScript」比8.879
（需要92.5/供給10.4件/日、有料率13%）。
willingness2: 無料記事想定。通知の仕組みを知っても、Slack Webhook URLの発行など
読者側の準備工程が別途残る。
artifact5: 締切時刻に走る時間主導トリガーの設定手順＋実行権限（誰の権限で動くか）の
確認手順＋全員一致の空き枠を検出してSlack Incoming WebhookにPOSTする通知コード全文を出せる。
次点: 「回答が集まるたびに即時通知する」形も検討したが、締切前に何度も通知が飛ぶと
読者側の運用が煩雑になるため、締切後の一括集計・通知に絞った。

今日これを選んだ理由: weekly-research が day: fri を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-29（土曜）に行った。market データは research/market/2026-08-29.md を当日取得
しているため、判断材料は最新である。**むしろ規定より新しい。**

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告14 | — |
| dedup.py | 最大類似度 0.353（notes/2026-08-28-trigger-slack/02-final.md） | — |
| reader-feedback | 実行していない（週1本のみ。月曜の記事に掛けた） | — |
| ethics-line（light） | 起動した（clear: true / round 1） | — |

### reader-feedback

- 実行していない


### ethics-line — 削除・修正した内容（全件）

**起動した。** `lint.py` の `needsAudit` が `true` だったため（CLAUDE.md ルール6）。

`auditor` による自動修正は 0 件（severity: block の検出が無かった）。


**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

`auditor` が人間の判断に残した項目が 3 件ある。

- L[9] [E3]「月曜に3件、火曜に5件、水曜に2件。回答が来るたびにSlackが鳴り、内容はそのつど「まだ全員揃っていません」という中間報告です。」
  - 直前の「回答が届くたびに通知する設計にすると、こんな流れになります」で仮定として枠取りされており、文法上も過去形ではない。ただし曜日ごとの件数が具体的なため、実際に運用したチャンネルのログとして読める余地がある。書き手はこのシステムを運用しておらず、profile.md の「体験」は空。例示の数値は demo の参加者名と同じ性質（2026-09-03-heatmap-view の review 判定と同格）なので自動修正しない
  - 対応: 自動修正しない。実運用の記録でないなら「たとえば月曜に3件、火曜に5件と届くとします」の形にすれば足りる。判断は人間に残す
- L[5, 11, 13] [E3]「何度も鳴る通知は誰も見なくなる / チャンネルのメンバーは、最初の数件は見ますが、途中から通知を開かなくなります。/ 既読はつくのに、内容を読まずに次の未読へ移るだけの操作になりがちです。」
  - 母数のない一般化（「誰も」）と、第三者の行動が実際にそうなったという効果の主張。書き手はこの通知を運用していないため観測に基づかない。加えて Slack のチャンネル投稿に「既読」表示は無く、書き手が観測しうる事象ではない。ただし通知疲れは設計上の一般的な論拠であり、嘘だと断定はできない
  - 対応: 自動修正しない。「開かれなくなりやすい設計である」など、観測ではなく設計上の帰結として書けば足りる。「既読はつくのに」は Slack の仕様に無い描写なので削除候補。判断は人間に残す
- L[187] [E3]「日程調整では、全員が空いている枠がそのまま見つかることのほうが少ないです。」
  - 頻度の主張（「〜のほうが少ない」）だが、母数となるデータも出典も無い。書き手の運用記録は profile.md に存在しない。一方で数値・成果の主張ではなく、後続の設計理由を導くための一般論にとどまる
  - 対応: 自動修正しない。「全員一致が出ない場合に備える」と条件の形にすれば頻度の主張を避けられる。判断は人間に残す


---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 収益額・作業時間の削減率・利用者数。いずれも `profile.md` の「書けないこと」に該当する
- **このシステムを実際に運用した結果。** 書き手は日程調整ヒートマップを運用しておらず、
  `profile.md` の「体験」セクションは空である。検証した観測として書ける事実が無いため、
  挙動はすべて仕様の説明として書いた
- `profile.md` の実績（記事45本 / フォロワー101人 / スキ中央値6.5）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | `00-theme.md` の sources で足りた。追加の web 検索は行っていない |
| `account-design` | サブカテゴリ③の既定タグは `NotebookLM` 固定だが、今週も材料が Gemini API 側だけであり在庫側で `GeminiAPI` に差し替えている。2週続けて同じ差し替えが発生した。account.md のハッシュタグ表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告14件・ブロックなし）

- L15 [heading-interval] 前の見出しから 215 文字（目安 300）。見出しが多すぎる
- L19 [sentence-length] 一文が 63 文字（目安 60）
- L25 [heading-interval] 前の見出しから 170 文字（目安 300）。見出しが多すぎる
- L33 [paragraph-length] 6 行続いている（目安 5）。空行を入れる
- L66 [heading-interval] 前の見出しから 240 文字（目安 300）。見出しが多すぎる
- L191 [unverified-claim] 人数の主張。profile.md に根拠が無ければ書けない（ethics-line を起動する）
- L194 [unverified-claim] 人数の主張。profile.md に根拠が無ければ書けない（ethics-line を起動する）
- L196 [heading-interval] 前の見出しから 248 文字（目安 300）。見出しが多すぎる
- L232 [heading-interval] 前の見出しから 119 文字（目安 300）。見出しが多すぎる
- L240 [heading-interval] 前の見出しから 182 文字（目安 300）。見出しが多すぎる
- L242 [sentence-length] 一文が 66 文字（目安 60）
- L248 [heading-interval] 前の見出しから 236 文字（目安 300）。見出しが多すぎる
- L258 [heading-interval] 前の見出しから 215 文字（目安 300）。見出しが多すぎる
- L264 [sentence-length] 一文が 83 文字（目安 60）

### dedup.py

- 既出記事との最大類似度 **0.353**（比較相手: notes/2026-08-28-trigger-slack/02-final.md）
- 週内の他記事との最大類似度 **0.247**（比較相手: 2026-08-31-schedule-intake）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが高くなることがある。
実測では 0.15〜0.25 に収まり、警告閾値 0.70 を大きく下回った。

**前週（2026-W35B・アンケート自由記述の要約）との比較が最も高い。** 同じ役割の回どうしは
骨格が似るためである。それでも 0.17〜0.35 にとどまったのは、中核処理の変換型を
「要約・分類」から「抽出・正規化」に変えたことが効いている（weekly-research の系統選定）。
