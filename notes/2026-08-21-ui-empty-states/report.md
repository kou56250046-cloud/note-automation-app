# 生成ログ — 2026-08-21-ui-empty-states

日付: 2026-08-17 生成 / 公開予定 2026-08-21（金・曜日の割り当てどおり）
種別: 無料記事
タイトル: AIが作った一覧画面が0件で崩れる。空・読み込み中・エラーを足す
カテゴリ: 生成AIで見せられる成果物を作る / AI×Webデザイン

---

## テーマ選定

引いたテーマ: `research/themes.md` の `2026-W34-05`（day: fri）

スコア: 強み5 / 市場性4 / 支払い意欲3 / artifact5 = **17/20**

選定理由:
- strength5: AI に画面を作らせると、データが理想的に揃った状態しか出てこない。
  ペルソナがスクリーンショットを撮るときに崩れるのは、たいていデータが0件のときである
- market4: UIデザイン 比7.255。UI Stack（5つの状態）を解説した記事は複数あるが
  （POSTD・Qiita・u-site.jp）、**いずれも設計論であり、AI の出力に後から足すという文脈の記事は見当たらない**
- artifact5: 3状態を含む after.html、AI への追加指示の全文、before/after のデモを出せる

次点1: 「UI Stack の5つの状態を解説する」— 情報としては上位互換だが、Partial state を含めると
解説記事に寄り実物が薄くなる。AI が最も落とす3つに絞ったほうが読者の画面が変わる
次点2: 「エラーメッセージの書き方」— 主題が文言になり、サブ①の「画面が変わる」条件から外れる

---

## 実物

- デモ: https://kou56250046-cloud.github.io/note-automation-app/demo/2026-08-21-ui-empty-states/
- `demo/before.html`（理想状態だけの一覧画面）/ `demo/after.html`（3状態をボタンで切り替え）
- **外部ライブラリ・CDN・外部フォント・外部画像を不使用。** 状態の切り替えは素の JS
- `alert()` / `confirm()` / `prompt()` を使っていない
- 本文にコードブロック2個（3状態の HTML/CSS/JS、AI に投げた追加指示の全文）

---

## ゲートの結果

- `lint.py`: **エラー0件 / 警告8件**（2650字 / コードブロック2）
- `dedup.py`: 既出14本と比較、最大類似度 **0.00**。重複なし
- `ethics-line`: **未起動**（`lint.py` の `needsAudit: false`）
- `reader-feedback`: 対象外（週1本の枠は月曜の記事に充てる）

### 差し戻し 1回（char-count / sentence-length）

初版は本文 **2433字** で下限2500を下回った。writer 側の目算は3350字だったが、
`lint.py` の実測（コードブロック除く）とは900字以上の開きがあった。
L62 に88文字の一文（デモURLを含む）もあった。**failures だけを渡して差し戻した。**

修正後: 2650字 / エラー0件。

### lint.py の警告（ブロックされなかったものも記録する）

`heading-interval` が8箇所残っている（L33 / L56 / L75 / L122 / L137 / L150 / L188 / L320 / L337）。
**構造上の必然として許容した。** 空・読み込み中・エラーの3状態をそれぞれ節に分ける構成のため、
1節あたりの文字数が短くなる。見出しを統合すると3状態の区切りが消える。

`sentence-length` の警告（L62 の88文字）は差し戻しで解消済み。

---

## 差別化（dedup 対策）

既存の `2026-08-11-ui-3points`（余白・階層・色数）と同じ「3箇所直す」型に見えるが、対象が違う。

- あちらは**見た目**を直す記事
- こちらは**画面の数**を増やす記事

「UI には5つの状態がある」という設計論から入らず、
「0件のときにスクリーンショットが撮れない」という症状から入っている。
**Partial state は扱わないと本文で明記した。**

結果、類似度 0.00。

---

## 書けなかったこと

`profile.md` に根拠がないため、以下は書いていない。

- 「見やすくなった」「反応が良くなった」等の成果の主張
- 「読み込み中のちらつきを抑えると体感速度が上がる」— 検証していないため

---

## weekly-research への申し送り

- `sources` の中に、**AI の出力へ「あとから3状態を足す」という文脈（設計論ではなく実装記事）に
  直接一致するものが無かった。** POSTD・Qiita・u-site.jp はいずれも一般的な UI 設計論で、
  AI コーディング前提の記事ではない
- この角度専用の一次情報が今後も薄い可能性がある。次にこの系列を深掘りするときは、
  英語クエリ（"AI generated UI empty state" など）を検索対象に足すと材料が増える見込み
