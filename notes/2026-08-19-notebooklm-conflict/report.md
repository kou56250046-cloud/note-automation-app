# 生成ログ — 2026-08-19-notebooklm-conflict

日付: 2026-08-17 生成 / 公開予定 2026-08-19（水・曜日の割り当てどおり）
種別: 無料記事
タイトル: NotebookLMの「要約して」が、出典ごとの食い違いを均してしまう話
カテゴリ: 生成AIで見せられる成果物を作る / NotebookLM・精度向上

---

## テーマ選定

引いたテーマ: `research/themes.md` の `2026-W34-03`（day: wed）

スコア: 強み4 / 市場性4 / 支払い意欲3 / artifact4 = **15/20**（今週の在庫で最低スコア）

**最低スコアだが曜日の割り当てどおり採用した。** サブカテゴリを週内で散らす方針
（`account.md` の週次配分）があり、水曜はサブ③の枠である。スコア順に引くと同じサブカテゴリが偏る。

選定理由:
- market4: `market/2026-08-17.md` の実測で NotebookLM は需要232.5 / 供給32.0件/日、比 **7.27（14タグ中1位）**。
  ただし総記事数25,662で、上位例（スライド生成・音声化）は既に厚い。
  食い違いを検出させる質問をテンプレート化した記事は見当たらないため4とした
- artifact4: 質問テンプレート全文と回答比較を出せる。**コードではないため5には届かない**

次点1: 「NotebookLM のスライド生成を制御する」— market は高いがプロンプト集が既に飽和（W33-08 の判定と同じ）
次点2: 「音声解説の使い方」— 成果物が音声で、note の記事内に実物を置けないため artifact 2

---

## 実物

- 食い違い検出用の**質問テンプレート全文**（4段構成: 主張の一覧化 → 出典ごとの数値の抜き出し →
  一致/不一致の仕分け → 不一致だけを残す）をコードブロックで掲載
- 「要約して」と投げた場合とテンプレートを投げた場合の回答構造の違いを、引用チップの差まで並置

---

## ゲートの結果

- `lint.py`: **エラー0件 / 警告4件**（3054字 / コードブロック1）
- `dedup.py`: 既出14本と比較、最大類似度 **0.00**。重複なし
- `ethics-line`: **未起動**（`lint.py` の `needsAudit: false`）
- `reader-feedback`: 対象外（週1本の枠は月曜の記事に充てる）

### lint.py の警告（ブロックされなかったものも記録する）

- L1 `sentence-length` 78文字 → **親側で2文に分割して解消**
- L25 `sentence-length` 67文字（目安60）— 許容した
- L109 `sentence-length` 62文字 — 出典URLを含む行。許容した
- L125 `sentence-length` 61文字 — 許容した
- L139 `heading-interval` 前の見出しから249文字（目安300）— 「今日できること」節。許容した

### 差し戻し 0回

---

## 差別化（dedup 対策）

サブ③で3本目になるため、既存2本と役割を明確に分けた。

- `2026-08-11-notebooklm-custom` = **出力側**（カスタム指示）
- `2026-08-11-notebooklm-source-format` = **投入側**（ソース整形）
- 本記事 = **質問の側**

「NotebookLM は嘘をつかない」で終わっている既存記事に対し、
**嘘ではなく食い違いを平らに均してしまう**点を症状として提示した。

「このテンプレートでできないこと」節を設け、正誤判定はしない / 外部情報は探しに行かない /
投入前のソース整形は扱わない、と適用範囲を明示している。

結果、類似度 0.00。

---

## 書けなかったこと

なし。この記事は数値・体験を必要としない構成にした。

第三者が出した精度の数値（uravation.com）は**出典を明記した引用にとどめ、自分の実測として書いていない。**
回答例は「こういう形で返ってくる」という構造の例示であり、実測値ではないことを本文で明示している。

---

## weekly-research への申し送り

特になし。`sources` 3件で本文を組み立てられた。追加の web 検索は行っていない。
