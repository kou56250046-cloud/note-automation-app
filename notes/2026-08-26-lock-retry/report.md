# 生成ログ — 2026-08-26-lock-retry

日付: 2026-08-24（生成日）/ 公開予定 2026-08-26
種別: 無料記事
連載: 2026-W35B-survey-digest（1つの自動化システムを7分割した水曜・失敗）
タイトル: 同時送信で二重処理、429で停止。GASの壊れ方と直し方をコードで
文字数: 2625 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: 二重実行と429エラーを防ぐ。壊れるアンケート集計コードと直したコード（themes.md の id: 2026-W35B-03）

スコア: 強み5 / 市場性4 / 支払い意欲2 / artifact5 = 16/20

選定理由:
strength5: 旧W35-02（LockServiceの二重実行）・旧W35-05（Gemini API 429の再試行）が
「連載の水（失敗時の扱い）の材料」としてlearnings.mdに再利用可と明記されており、
手元の材料をそのまま使える。
market4: 「GAS」比6.301（需要207.0/供給32.9件/日、有料率11%）、「GoogleAppsScript」比4.407
（需要90.0/供給20.4件/日、有料率10%）。
willingness2: 無料記事想定。トラブル対処は知って終わりに近く、成果物の受け渡しは発生しない。
artifact5: 「同じ回答を2回処理してしまうコード→LockServiceのtryLockで直したコード」と
「Gemini 429で処理が止まるコード→指数バックオフで直したコード」の2対を出せる。
次点は無し（この2本の材料は既に確定しており、他候補と比較する必要がなかった）。

今日これを選んだ理由: weekly-research が day: wed を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-24（月曜）に行った。market データは research/market/2026-08-24.md を当日取得
しているため、判断材料は最新である。

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告8 | — |
| dedup.py | 最大類似度 0.21（2026-08-24-form-intake） | — |
| reader-feedback | 実行されていない（週1本のみ。月曜の記事に掛けた） | — |
| ethics-line（light） | **起動していない**（lint.py の needsAudit が false） | — |

### reader-feedback

- 実行されていない

### ethics-line — 削除・修正した内容（全件）

**起動していない。** `lint.py` の `needsAudit` が `false` だったため（CLAUDE.md ルール6）。

`lint.py` は E5（断定表現）を `voice.md` の `ng` で、E3（裏付けのない実績）を
`unverified-claim`（金額・割合・人数・期間・フォロワー数）で一次判定する。
どちらも検出がなかったので `auditor` を呼んでいない。

**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

- なし

---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 執筆エージェントからの報告では該当なし。この記事は手順・設計・コードで構成されており、
  成果の数値主張を必要としなかった
- `profile.md` の実績（記事33本 / フォロワー94人 / スキ中央値8）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | https://developers.google.com/apps-script/reference/lock/lock-service を含む sources 2件で足りた |
| `account-design` | サブカテゴリ③の既定タグが `NotebookLM` 固定になっている。材料が Gemini API 側だけの週では使えず、今週は在庫側で `GeminiAPI` に差し替えた。account.md 246行目〜の表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告8件・ブロックなし）

- L101 [sentence-length] 一文が 64 文字（目安 60）
- L137 [sentence-length] 一文が 67 文字（目安 60）
- L156 [sentence-length] 一文が 63 文字（目安 60）
- L160 [sentence-length] 一文が 66 文字（目安 60）
- L162 [heading-interval] 前の見出しから 98 文字（目安 300）。見出しが多すぎる
- L168 [heading-interval] 前の見出しから 98 文字（目安 300）。見出しが多すぎる
- L170 [sentence-length] 一文が 82 文字（目安 60）
- L171 [sentence-length] 一文が 81 文字（目安 60）

### dedup.py

- 最大類似度 **0.21**（比較相手: 2026-08-24-form-intake）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが構造的に高くなる。
実測ではいずれも 0.10〜0.25 で、警告閾値 0.70 を大きく下回った。
