# 生成ログ — 2026-08-27-survey-dashboard

日付: 2026-08-24（生成日）/ 公開予定 2026-08-27
種別: 無料記事
連載: 2026-W35B-survey-digest（1つの自動化システムを7分割した木曜・画面）
タイトル: アンケート結果カードを色数3色と並び1点で直したbefore/after
文字数: 2906 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: アンケート結果を、色数と並びの2箇所だけ直して見せられるダッシュボードにする（themes.md の id: 2026-W35B-04）

スコア: 強み5 / 市場性5 / 支払い意欲3 / artifact5 = 18/20

選定理由:
strength5: account.md「木・土は必ずサブ①」の指定に直接対応する。HTML Serviceでの
ダッシュボード作りは第2版から続く得意領域。
market5: 「UIデザイン」は比5.681（需要81.0/供給14.3件/日、有料率6%）で今回の実測でも
上位。GAS界隈で「スプレッドシートの画面のまま」の記事が多い中、自動化に顔を付ける
交差点が空いているという account.md の判断根拠と一致する。
willingness3: カード型ダッシュボードは「見せられる形」を丸ごと再現でき、行動（自分の
シートに貼る）に直結しやすい。
artifact5: テンプレ丸出しの一覧表示（before）→ カテゴリ別カード＋代表コメント表示（after）
のデモと、HTML Service全文を出せる。
次点は「円グラフでアンケート結果を可視化するダッシュボード」。既出記事「AIが出した
ダッシュボードを、色数・目盛り・並びの3箇所で読める形にする」と実物（グラフの
before/after）が重複するため、グラフではなくカード型に差し替えて採用した。

今日これを選んだ理由: weekly-research が day: thu を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-24（月曜）に行った。market データは research/market/2026-08-24.md を当日取得
しているため、判断材料は最新である。

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告5 | — |
| dedup.py | 最大類似度 0.20（2026-08-30-survey-kit） | — |
| reader-feedback | 実行されていない（週1本のみ。月曜の記事に掛けた） | — |
| ethics-line（light） | **起動していない**（lint.py の needsAudit が false） | — |

### reader-feedback

- 実行されていない

### ethics-line — 削除・修正した内容（全件）

**起動していない。** `lint.py` の `needsAudit` が `false` だったため（CLAUDE.md ルール6）。

`lint.py` は E5（断定表現）を `voice.md` の `ng` で、E3（裏付けのない実績）を
`unverified-claim`（金額・割合・人数・期間・フォロワー数）で一次判定する。
どちらも検出がなかったので `auditor` を呼んでいない。

**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

- なし

---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 執筆エージェントからの報告では該当なし。この記事は手順・設計・コードで構成されており、
  成果の数値主張を必要としなかった
- `profile.md` の実績（記事33本 / フォロワー94人 / スキ中央値8）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | https://ai.google.dev/gemini-api/docs/structured-output を含む sources 1件で足りた |
| `account-design` | サブカテゴリ③の既定タグが `NotebookLM` 固定になっている。材料が Gemini API 側だけの週では使えず、今週は在庫側で `GeminiAPI` に差し替えた。account.md 246行目〜の表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告5件・ブロックなし）

- L21 [sentence-length] 一文が 66 文字（目安 60）
- L67 [sentence-length] 一文が 96 文字（目安 60）
- L130 [heading-interval] 前の見出しから 0 文字（目安 300）。見出しが多すぎる
- L195 [heading-interval] 前の見出しから 0 文字（目安 300）。見出しが多すぎる
- L217 [heading-interval] 前の見出しから 204 文字（目安 300）。見出しが多すぎる

### dedup.py

- 最大類似度 **0.20**（比較相手: 2026-08-30-survey-kit）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが構造的に高くなる。
実測ではいずれも 0.10〜0.25 で、警告閾値 0.70 を大きく下回った。
