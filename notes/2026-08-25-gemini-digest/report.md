# 生成ログ — 2026-08-25-gemini-digest

日付: 2026-08-24（生成日）/ 公開予定 2026-08-25
種別: 無料記事
連載: 2026-W35B-survey-digest（1つの自動化システムを7分割した火曜・中核）
タイトル: 自由記述の要約をGeminiで返す。JSON型を固定するプロンプトとコード全文
文字数: 2824 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: アンケートの自由記述をGeminiで要約・分類する。プロンプトと呼び出しコードの全文（themes.md の id: 2026-W35B-02）

スコア: 強み5 / 市場性4 / 支払い意欲2 / artifact5 = 16/20

選定理由:
strength5: account.md サブ3「Gemini APIは自動化の材料として扱う」にど真ん中で対応する。
market4: 「GAS」は比6.301（需要207.0/供給32.9件/日、有料率11%）で全体の主軸。ハッシュタグには
「GeminiAPI」（比21.446、需要27.0/供給1.3件/日、有料率10%、総563件・計測タグ中1位）を採用した。
総563件は他タグより母数が小さいニッチ帯である点をここに明記する。内容（Gemini呼び出しコード）
と直接一致するため、NotebookLM（既定タグ）ではなくこちらを主軸にした（ヘッダーの差し戻し対応参照）。
willingness2: 無料記事想定。プロンプトを知っても、自分のシートに繋ぎ込む工程がまだ残る。
artifact5: 要約＋カテゴリ＋感情スコアを返すプロンプト全文と、responseSchemaでJSON型を固定した
UrlFetchApp呼び出しコード全文を出せる。
次点は「問い合わせ返信文自動生成（多言語対応）」。既出記事「問い合わせをGeminiで自動仕分けする」
と中核処理が同型でdedup.py に高類似度で引っかかるリスクが高く、この系統自体を選ばなかった。

今日これを選んだ理由: weekly-research が day: tue を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-24（月曜）に行った。market データは research/market/2026-08-24.md を当日取得
しているため、判断材料は最新である。

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告12 | — |
| dedup.py | 最大類似度 0.27（2026-08-18-gas-classify） | — |
| reader-feedback | 実行されていない（週1本のみ。月曜の記事に掛けた） | — |
| ethics-line（light） | **起動していない**（lint.py の needsAudit が false） | — |

### reader-feedback

- 実行されていない

### ethics-line — 削除・修正した内容（全件）

**起動していない。** `lint.py` の `needsAudit` が `false` だったため（CLAUDE.md ルール6）。

`lint.py` は E5（断定表現）を `voice.md` の `ng` で、E3（裏付けのない実績）を
`unverified-claim`（金額・割合・人数・期間・フォロワー数）で一次判定する。
どちらも検出がなかったので `auditor` を呼んでいない。

**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

- なし

---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 執筆エージェントからの報告では該当なし。この記事は手順・設計・コードで構成されており、
  成果の数値主張を必要としなかった
- `profile.md` の実績（記事33本 / フォロワー94人 / スキ中央値8）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | https://ai.google.dev/gemini-api/docs/structured-output を含む sources 1件で足りた |
| `account-design` | サブカテゴリ③の既定タグが `NotebookLM` 固定になっている。材料が Gemini API 側だけの週では使えず、今週は在庫側で `GeminiAPI` に差し替えた。account.md 246行目〜の表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告12件・ブロックなし）

- L21 [heading-interval] 前の見出しから 261 文字（目安 300）。見出しが多すぎる
- L37 [heading-interval] 前の見出しから 275 文字（目安 300）。見出しが多すぎる
- L50 [sentence-length] 一文が 72 文字（目安 60）
- L92 [heading-interval] 前の見出しから 71 文字（目安 300）。見出しが多すぎる
- L96 [sentence-length] 一文が 69 文字（目安 60）
- L164 [sentence-length] 一文が 89 文字（目安 60）
- L185 [sentence-length] 一文が 74 文字（目安 60）
- L192 [sentence-length] 一文が 62 文字（目安 60）
- L202 [sentence-length] 一文が 67 文字（目安 60）
- L214 [sentence-length] 一文が 71 文字（目安 60）
- L216 [sentence-length] 一文が 64 文字（目安 60）
- L220 [heading-interval] 前の見出しから 195 文字（目安 300）。見出しが多すぎる

### dedup.py

- 最大類似度 **0.27**（比較相手: 2026-08-18-gas-classify）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが構造的に高くなる。
実測ではいずれも 0.10〜0.25 で、警告閾値 0.70 を大きく下回った。
