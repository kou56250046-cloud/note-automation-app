# 生成ログ — 2026-08-30-survey-kit

日付: 2026-08-24（生成日）/ 公開予定 2026-08-30
種別: 有料note（`productType: tool`・道具型）
連載: 2026-W35B-survey-digest（1つの自動化システムを7分割した日曜・完成版）
タイトル: アンケートの自由記述を、シート名とカテゴリだけ差し替えて自動要約する
価格: 1,480円
文字数: 無料部分（`02-letter.md`）2,062字 / 有料部分（`03-draft.md`）5,520字

---

## テーマ選定

引いたテーマ: アンケート自由記述 自動要約ダッシュボード完成版一式（themes.md の id: `2026-W35B-07`）

スコア: 強み5 / 市場性4 / 支払い意欲5 / artifact5 = 19/20

選定理由（themes.md の rationale より）:

- strength5: 連載の完成版そのもの。月〜土で出した6本の成果物をそのまま束ねられる
- market4: `market/2026-08-24.json` の `GAS` は比6.301・有料率11%、`GoogleAppsScript` は比4.407・有料率10%、`GeminiAPI` は比21.446・有料率10%。クラスタ#6（プロンプト/AI/マジクラ、35記事、スキ中央値206、比5.8857）が「型を渡すテンプレート」の読まれる帯であることを示す
- willingness5: 手順を丸ごとテンプレートとして渡せる。**4以上を満たすため paid に割り当てた**
- artifact5: フォーム受信〜Gemini要約〜ダッシュボード表示〜Slack通知までの全コードと差し替え表を出せる

次点: 「経費精算レシート自動チェックシステム」の完成版。系統自体を `weekly-research` の段階で落としている（7分割に収まらない見込み・金額を扱うため ethics-line のリスク増）。`learnings.md` に記録済み。

**生成日が規定とずれている。** `weekly-build` は日曜実行を前提にしているが、実際の生成は 2026-08-24（月曜）に行った。market データは `research/market/2026-08-24.md` を当日取得しているため、判断材料は最新である。

---

## 商品設計の判断

### productType

`tool`（道具型）で確定。`product-concept` の判定手順ステップ2で確定している。

- `profile.md` の実績: **あり**（記事33本 / フォロワー94人 / スキ中央値8）
- 同ジャンルか: **違う**。`profile.md` 40行目に「日常・脳科学ジャンルでの数値である。AI技術ジャンルの実績ではない」と明記
- 自分以外の結果: **なし**（`profile.md` 78行目）
- → 実績はあるが別ジャンルのものであり、この商品の主張を支えられない。道具型の再現性は成果物が手元で動くかどうかで担保する

### 価格

1,480円（`pricing.json`）。

- `market/2026-08-24.json` の `overall.priceMedian` は **500円**（08-06 時点の800円から下落）
- 情報密度「高」の倍率 2〜3.5倍 → 算出レンジ 1,000〜1,750円
- `account.md` 207行目の想定価格帯 1,480〜2,980円 との重なりは 1,480〜1,750円
- `profile.md` に販売実績が無いため、その**下限側**を採った
- 同 json のタグ別 `priceMedian` は全タグ `null`（取得できていない）。価格の根拠は `overall.priceMedian` のみに置いている

### 値上げ予定は無い

`pricing.json` の `priceSchedule` は空配列。**レターに期限・先着・数量限定を1つも書いていない。**

C3（今買う理由）は記事構造が作る設計（CLAUDE.md 第3版）。

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py（`02-letter.md`） | エラー0 / 警告2 | — |
| lint.py（`03-draft.md`） | エラー0 / 警告12 | **1周**（字数不足） |
| dedup.py | 最大類似度 0.23（`2026-08-25-gemini-digest`） | — |
| **letter-audit** | **合格 16/16** | **1周**（round 1: 14/16） |
| **ethics-line（full）** | **`clear: true` / block 0件** | 0周 |
| コード実行検証 | 構文・定数参照・関数接続すべて通過 | — |

### draft-writing への差し戻し（1周）

`03-draft.md` の初稿が **4,437字**で、`account.md` 203行目が定める有料部分5,000〜8,000字の**下限を下回っていた。**

**`lint.py` はこれを検出していない。** 同スクリプトの `minCharCount: 2500` は無料記事向けの値で、有料部分の下限を見ていないためである（後述の「上流への申し送り」参照）。

セクション別の字数を測ったところ、コードを載せたセクション2〜7が118〜373字と薄く、コードを貼っただけで設計判断の説明がほぼ無い状態だった。**コードを増やさず、なぜその形にしたかを各セクションに100〜200字ずつ足させた。** 結果 5,520字。

差し戻し時は該当セクションのみを指示し、セクション1・8・9・10 とコードブロックには触れさせていない。**修正後にコードブロック6本すべての SHA-256 を初稿と突き合わせ、1文字も変わっていないことを確認した。**

### letter-audit（round 1: 14/16 → round 2: 16/16）

round 1 の×は2件。

- **A1**（L54-83, L87-91）: 再現性ブロックが2つ連続していた（成果物の開示 → 不便だった状況）。あいだに理想の未来が無く、実物を見せた直後に手作業の話が続くため、読者が「それで自分はどうなるのか」に戻れないまま反論処理へ入っていた
  → 「不便だった状況」ブロックを恐怖ブロックの直後へ移動。並びを 理想の未来→再現性→理想の未来→再現性→恐怖→再現性→理想の未来→再現性 にした
- **C3**（L99, L106）: 「なぜ金を払うか」（無料6本との差分）は書かれていたが、「なぜ後日ではなく今日か」が無かった。後回しにして失うものが1つも書かれておらず、L106「部品はもう読めます」が先送りの口実そのものを読者に渡していた
  → **期限を1つも足さずに解消。** 「無料6本を読んだ直後は、どのファイルが何を担当しているかがまだ頭に入っている。時間が空くと読み直すところに戻る」という、読者がいま持っている状態が目減りするという書き方にした

差し戻しは `failures` 2件のみを渡し、○だった14項目には触れさせていない。

**C3 の判定基準に食い違いがあった。** `letter-audit/SKILL.md` 119行目は×の条件を「期限・値上げ・特典終了のいずれも無い」としているが、CLAUDE.md 第3版は「C3 を記事構造が作る」と定めている。**どちらを採るかを親が決めるとゲートを操作することになるため、`critic` に両方を提示して判断させた。** 判定者は後者を採り、根拠を `04-audit.md` に記録している。

なお round 1 の `critic` は、判定を終えて `audit.json` を書いた直後にセッション上限で停止し、`04-audit.md` を出力できなかった。**判定内容には手を加えず、`audit.json` からレポートを起こした**（round 2 で上書き済み）。

### ethics-line — 削除・修正した内容（全件）

**`block` は0件。本文は1文字も修正していない。**

- モード: `full` / 検査項目: E1・E2・E3・E4・E5（全5項目）
- rounds: 1

| 項目 | block | review |
|---|:---:|:---:|
| E1 偽の限定 | 0 | 0 |
| E2 盛った恐怖 | 0 | 2 |
| E3 裏付けのない実績 | 0 | 0 |
| E4 書きっぱなしの弱点 | 0 | 0 |
| E5 断定表現 | 0 | 1 |

**E1 の検証:** `letter-meta.json` の `deadlineClaims` は空配列。本文を「限定」「先着」「値上げ」「◯部」「終了」で走査した結果も0件で、**宣言と本文が一致している。**

**E3 の検証:** `profile.md` の実測値（記事33本 / フォロワー94人 / スキ中央値8）は `02-letter.md`・`03-draft.md` のどちらにも引用されていない。したがって「日常・脳科学ジャンルの数値である」という前提を落とした引用は発生していない。本文中の数値はすべて商品構成（5ファイル・7項目・1,480円）と技術パラメータ（-2〜2・10秒・6分）である。

### 人間に回した項目（severity: review）

**3件。自動修正していない。**

1. **E2 — `02-letter.md` L38-41**
   引用: 「渡せないまま時間が経つと…自分がいないと、誰もそのシートを読めない状態が続きます。休んだ日、誰かが『これ、どう見ればいいですか』と聞きに来ます。」
   判定理由: `profile.md` の「体験」が（まだない）で、この地獄に対応する本人の体験が記録されていない。ただし読者の職場に起きうる条件付きの未来として書かれており、書き手が体験したという主張ではないため嘘と断定できない。恐怖は1つで、L43「まだ、そうなる前に直せます。」により回避可能な位置に戻してある

2. **E2 — `02-letter.md` L105**
   引用: 「時間が空くと、そこから読み直すところに戻ります。」
   判定理由: `letter-meta.json` は `fearCount: 1` と宣言しているが、これは2つ目の損失フレームとして読める。内容自体は嘘ではなく、焦らせる演出は白なので削除の根拠にならない。**2つ目の恐怖と数えるかは人間の判断**
   補足: この行は `letter-audit` の C3 を解消するために追加したものである。恐怖として数えるなら A1/B1 と C3 が構造的に衝突することになり、**上流（`letter-audit` の C3 と `ethics-line` の E2 の境界定義）の問題である**

3. **E5 — `03-draft.md` L325・326・328・329**
   引用: 「Geminiは、この配列にない文字列を返せません。」「返答は常にJSONです。」「JSON.parseが失敗する心配はありません。」
   判定理由: 医療・投資・収益のいずれでもないため E5 の対象領域そのものではないが、無条件の断定。トークン上限打ち切りや安全フィルタでの遮断では成り立たない場合がある。`profile.md` の「書けないこと」に「『再現性が確認された』という主張」があり検証の裏付けがない。嘘とは断定できないため review
   **親の確認:** 同梱コードでは `JSON.parse(text)` が `summarizePendingRows` の try/catch 内で呼ばれており、失敗した行は `STATUS_ERROR` になる。**コードは本文の主張より慎重であり、機能上の欠陥ではない。** 本文の書き方が言い過ぎているという指摘である

---

## 同梱コードの検証（weekly-build 179行目の要求）

`03-draft.md` からコードブロックを抽出して検査した。GAS の API は Node で実行できないため、実行の代わりに以下を行った。

- **構文検査**: `config.gs` / `intake.gs` / `summarize.gs` / `guard.gs` / `notify.gs` の5本すべてが `node --check` を通過
- **定数の定義と参照**: `SHEET_NAME` `FREE_TEXT_COLUMN_HEADER` `CATEGORIES` `NOTIFY_THRESHOLD` `GEMINI_MODEL` `GEMINI_ENDPOINT` `STATUS_PENDING` `STATUS_DONE` `STATUS_ERROR` `LOCK_WAIT_MS` `MAX_RETRY` `RETRY_BASE_WAIT_MS` の12個すべてが `config.gs` でのみ定義され、参照側と一致。二重定義・定義漏れなし
- **ファイル間の関数呼び出し**: `summarizePendingRows`（intake→summarize）/ `withLock_`・`callGeminiWithRetry_`（summarize→guard）/ `notifySlackIfNeeded_`（summarize→notify）/ `getGeminiApiKey_`（guard→config）/ `getSlackWebhookUrl_`（notify→config）の6本すべてが定義先に解決
- **`responseSchema`**: 形式が正しく、`enum: CATEGORIES` が `config.gs` を正しく参照
- **`index.html`**: 外部リソース参照が0件（GitHub Pages の CSP に適合）
- **APIキーの扱い**: `GEMINI_API_KEY` と `SLACK_WEBHOOK_URL` はどちらも `PropertiesService` のスクリプトプロパティから取得しており、コードへの直書きなし

**未検証の部分:** Gemini API と Slack Webhook への実際の疎通は行っていない。API キーを持たないためである。

---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- **販売実績・購入者の結果**（`profile.md` の「第三者の結果」が（まだない））。道具型のためレターの再現性は成果物の開示で担保しており、実績を必要としない構造にしてある
- **時間削減・精度の数値**（「差し替えは◯分で終わる」「分類精度◯%」）。実測していないため書いていない
- **書き手自身の体験談**（`profile.md` の「体験」が（まだない））。上記 review 1件の原因でもある
- `profile.md` の実測値（記事33本 / フォロワー94人）は旧ジャンルの数値のため、この商品では一切引用していない

> このリストは `profile-accumulator` が実績を追記すると短くなる。
> **販売数と購入者の結果が入れば、次の商品から 1,980円 を検討できる**（`pricing.json` の `raiseCondition`）。

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| **`scripts/lint.py`** | **有料部分の字数下限を検出できない。** `minCharCount: 2500` は無料記事向けの値であり、`03-draft.md` が 4,437字（下限5,000字）でもエラーにならなかった。`meta.json` の `type` を見て、`paid` の `03-draft.md` には 5000〜8000字を適用する分岐が要る |
| **`.claude/skills/editorial/letter-audit/SKILL.md`** | **C3 の条件文が第3版に追従していない。** 119行目は×の条件を「期限・値上げ・特典終了のいずれも無い」としているが、CLAUDE.md 第3版は「C3 を記事構造が作る」と定めている。このままだと、期限を持たない商品が構造的に落ちるか、判定者ごとに結論が割れる |
| **`ethics-line` と `letter-audit` の境界** | C3 を解消するために書いた「後回しにすると失うもの」（L105）が、E2 で2つ目の恐怖として `review` に上がった。**C3 を満たす書き方が E2 に触れる構造**になっている。どちらかの定義に、損失フレームと恐怖の線引きを書く必要がある |
| `scripts/dedup.py` | 人間向け出力の「最大類似度」が、警告閾値0.70未満のマッチを `matches` に入れない実装のため一律 `0.00` と表示される（186行目・195行目）。ブロック判定は正しいが、CLAUDE.md が求める「ブロックされなかった警告も記録する」に対して実値が残らない |
| `account-design` | サブカテゴリ③の既定タグが `NotebookLM` 固定になっている。材料が Gemini API 側だけの週では使えず、今週は在庫側で `GeminiAPI`（比21.446・計測タグ中1位）に差し替えた。`account.md` 246行目〜の表の見直しを推奨する |
| `weekly-research` | `sources` 5件で本文・レターとも足りた。不足なし |

---

## 機械判定の詳細

### lint.py — `02-letter.md`（エラー0 / 警告2）

- L91 `sentence-length` 一文が89文字（目安60）— デモURLを含む行
- L112 `unverified-claim` 金額の主張（「1,480円」の一部を検出）→ **誤検出**。価格表記であり実績主張ではない。`ethics-line` の E3 で0件と確定している

### lint.py — `03-draft.md`（エラー0 / 警告12）

すべて `sentence-length`（61〜98字）と `heading-interval`。ブロックなし。

### dedup.py

- 検査対象: `02-letter.md`（有料部分 `03-draft.md` は `.gitignore` 対象のため `collect_published()` の対象外）
- 最大類似度 **0.23**（`2026-08-25-gemini-digest`）。次いで 0.21（`2026-08-24-form-intake`）、0.21（`2026-08-28-trigger-slack`）
- ブロック閾値0.85 / 警告閾値0.70 のいずれも下回る
- **同一システムを7分割した連載のため、週内の相互類似度が既出記事より高くなる構造にある。** 実測では0.10〜0.30に収まった
