# 生成ログ — 2026-09-03-heatmap-view

日付: 2026-08-29（生成日）/ 公開予定 2026-09-03
種別: 無料記事
連載: 2026-W36-schedule-heatmap（1つの自動化システムを7分割した木曜・画面）
タイトル: 全員の空き状況を、色の濃淡で一目にするヒートマップダッシュボード
文字数: 2679 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: 全員の空き状況を、色の濃淡で一目にするヒートマップダッシュボード（themes.md の id: 2026-W36-04）

スコア: strength 5 / market 5 / willingness 3 / artifact 5 = 18

選定理由:
strength5: account.md「木・土は必ずサブ①」の指定に直接対応する。HTML Serviceでの
ダッシュボード作りは第2版から続く得意領域。
market5: market/2026-08-29.md の「UIデザイン」は需要81.0 / 供給13.2件/日で比6.123
（有料率8%、総15,169件）。今回の実測でも上位帯。GAS界隈で「スプレッドシートの画面のまま」
の記事が多い中、自動化に顔を付ける交差点が空いているというaccount.mdの判断根拠と一致する。
willingness3: ヒートマップは「全員が空いている枠が一目でわかる」形を丸ごと再現でき、
行動（自分のシートに貼る）に直結しやすい。
artifact5: 名前を縦に列挙しただけのbefore（ただの一覧表）→曜日×時間帯のグリッドで
空き人数を色の濃淡（CSS grid + HSLのintensity変数）で見せるafterのデモと、
HTML Service全文を出せる。CSS gridでのヒートマップ実装（HSLのhueを強度で変える手法、
intensity変数を0〜1で持たせる手法）は一次情報で確認済み。
次点: 「棒グラフで各曜日の空き人数を示す」も検討したが、既出記事「AIが出したダッシュボードを、
色数・目盛り・並びの3箇所で読める形にする」（W34-01）と実物（グラフのbefore/after）が
重複するため、グリッドヒートマップに差し替えて採用した。

今日これを選んだ理由: weekly-research が day: thu を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-29（土曜）に行った。market データは research/market/2026-08-29.md を当日取得
しているため、判断材料は最新である。**むしろ規定より新しい。**

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告10 | — |
| dedup.py | 最大類似度 0.172（notes/2026-08-27-survey-dashboard/02-final.md） | — |
| reader-feedback | 実行していない（週1本のみ。月曜の記事に掛けた） | — |
| ethics-line（light） | 起動した（clear: true / round 2） | — |

### reader-feedback

- 実行していない


### ethics-line — 削除・修正した内容（全件）

**起動した。** `lint.py` の `needsAudit` が `true` だったため（CLAUDE.md ルール6）。

`auditor` が修正した箇所は 1 件である。

- L29 [E3]
  - before: 最初、人数をそのまま0〜5の連続値としてHSLの明度に流し込みました。3人と4人の色がほとんど同じに見え、見比べないと違いが分かりませんでした。
  - after: 人数をそのまま0〜5の連続値としてHSLの明度に流し込むと、1段階あたりの明度差は9ポイント程度にしかなりません。3人と4人のように隣り合う値の差は、並べて見比べないと読み取りにくい幅にとどまります。
  - 理由: 検証した観測の主張を、記事内の CSS（calc(94% - intensity * 46%)、5人で1段階あたり46/5=9.2ポイント）から導ける仕様の説明に置き換えた。段落の他の文には触れていない


**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

`auditor` が人間の判断に残した項目が 1 件ある。

- L[15, 17] [E3]「回答が集まるたびに、私は名前を1人ずつ見ていました。/ 佐藤さんは月の午後と火の午前が空いている。鈴木さんは……と、6人分の文章を頭の中で重ねていました。」
  - 一人称・過去形で書かれた運用体験。ただし SKILL の線引き表では「毎回手でコピペしていた」型の、数値・成果を伴わない作業の面倒さの描写は E3 として検出しないとされている。ここも成果や効果の主張ではない。一方で書き手がこの日程調整を実際に回した記録は profile.md に無く、参加者名（佐藤・鈴木・高橋）も例示である。削るとフックが成立しなくなるため自動修正しない
  - 対応: 自動修正しない。実運用の体験でないなら「〜する必要があります」の形に直せば足りる。判断は人間に残す


---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 収益額・作業時間の削減率・利用者数。いずれも `profile.md` の「書けないこと」に該当する
- **このシステムを実際に運用した結果。** 書き手は日程調整ヒートマップを運用しておらず、
  `profile.md` の「体験」セクションは空である。検証した観測として書ける事実が無いため、
  挙動はすべて仕様の説明として書いた
- `profile.md` の実績（記事45本 / フォロワー101人 / スキ中央値6.5）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | `00-theme.md` の sources で足りた。追加の web 検索は行っていない |
| `account-design` | サブカテゴリ③の既定タグは `NotebookLM` 固定だが、今週も材料が Gemini API 側だけであり在庫側で `GeminiAPI` に差し替えている。2週続けて同じ差し替えが発生した。account.md のハッシュタグ表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告10件・ブロックなし）

- L46 [unverified-claim] 人数の主張。profile.md に根拠が無ければ書けない（ethics-line を起動する）
- L52 [heading-interval] 前の見出しから 229 文字（目安 300）。見出しが多すぎる
- L68 [sentence-length] 一文が 92 文字（目安 60）
- L72 [sentence-length] 一文が 66 文字（目安 60）
- L74 [sentence-length] 一文が 62 文字（目安 60）
- L145 [heading-interval] 前の見出しから 0 文字（目安 300）。見出しが多すぎる
- L248 [heading-interval] 前の見出しから 0 文字（目安 300）。見出しが多すぎる
- L258 [heading-interval] 前の見出しから 189 文字（目安 300）。見出しが多すぎる
- L268 [heading-interval] 前の見出しから 198 文字（目安 300）。見出しが多すぎる
- L274 [heading-interval] 前の見出しから 118 文字（目安 300）。見出しが多すぎる

### dedup.py

- 既出記事との最大類似度 **0.172**（比較相手: notes/2026-08-27-survey-dashboard/02-final.md）
- 週内の他記事との最大類似度 **0.195**（比較相手: 2026-09-04-slot-notify）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが高くなることがある。
実測では 0.15〜0.25 に収まり、警告閾値 0.70 を大きく下回った。

**前週（2026-W35B・アンケート自由記述の要約）との比較が最も高い。** 同じ役割の回どうしは
骨格が似るためである。それでも 0.17〜0.35 にとどまったのは、中核処理の変換型を
「要約・分類」から「抽出・正規化」に変えたことが効いている（weekly-research の系統選定）。
