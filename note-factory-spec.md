# note-factory 仕様書 v3.0

Claude Code で「ひとり出版社」を組み、リサーチから記事生成までを全自動化するシステム

作成日: 2026-08-05 / 主軸変更: 毎日投稿モデルへの再設計、リサーチの週次集約、商品の型分岐、ブラウザ自動操作の廃止

> ## ⚠ 2026-08-21 の改訂（第3版・自動化への転換）で変わった点
>
> - 主カテゴリを **「GAS と生成AI で他人に渡せる自動化システムを作る」**に改訂
> - サブカテゴリを ①見せられる自動化 ②自動化フローの設計思想 ③差し替えて使うテンプレート に変更
> - **週を「1つの自動化システムを7分割した連載」にした。** 日曜の有料noteは
>   月〜土の完成版一式であり、`letter-audit` の C3（今買う理由）を記事構造が作る
> - `weekly-research` は在庫を**7本単位**で仕込む（端数補充を廃止）。`systemId` を追加
> - **「自動化」「業務効率化」をハッシュタグの主軸にしない**（需要÷供給 0.031）。
>   名乗りは `GAS` `UIデザイン` `NotebookLM` 側から行う
> - `research/market/hashtags.txt` を差し替え（Canva を外し、
>   スプレッドシート / GoogleWorkspace / GeminiAPI / 自動化 を追加）
> - 想定価格帯を 980〜2,980円 → **1,480〜2,980円**（連載の完成版一式は単発より価値が高い）
>
> ## ⚠ 2026-08-07 の改訂で変わった点
>
> **この仕様書は v3.0 時点のものである。正典は [CLAUDE.md](./CLAUDE.md)。**
> 経緯は [knowledge/learnings.md](./knowledge/learnings.md) の「カテゴリ改訂の記録」にある。
>
> - 主カテゴリを「生成AIで人に見せられる成果物を作る」に改訂（実測で旧カテゴリの需要÷供給が 0.064 と判明）
> - 無料記事を **2500〜4000字**に拡張し、**実物（プロンプト全文・コード全文・before/after）を必須化**
> - `reader-feedback` を毎回 → **週1本**、無料記事の `ethics-line` を **`lint.py` の検出時のみ**に変更
> - `scripts/note_market.py` と `market-research` Actions を追加（需要と供給を**トークン0**で実測）
> - `scripts/build-demo.mjs` を追加（before/after を Pages でライブ配信）
> - `thumbnail-prompt` を追加。記事から見出し画像のプロンプトを組み立て、
>   プレビューからコピーして Gemini に貼る（画像は生成しない）
> - `/note-review` を追加。`revenue-maximizer` を手動で回せる
> - ダッシュボードをデモデータから**実測値**に切り替えた。
>   `note_market.py --write-dashboard` → `encrypt.mjs --from` で ENC に埋め込む。
>   **`data/` は Pages に配信しない**（`history.jsonl` にフォロワー数が平文で入るため）
> - スキルは 16/16、コマンドは 6/6 で完了

---

## 0. v2.0 からの変更点

v2.0 は「週1本の有料note を26スキルのフルパイプラインで作る」設計だった。v3.0 は**週7本を出す**設計に変える。

| 論点 | v2.0 | v3.0 | 理由 |
|---|---|---|---|
| 投稿頻度 | 週1本（有料） | **週7本**（無料5＋有料2） | アカウントを育てるには継続が要る |
| リサーチ | 日次＋週次×2 | **週1回に集約** | 日次リサーチだけで月2,000円相当。ここが最大の消費源だった |
| 初期設定 | `/note-setup` でヒアリング | **`account-research` で自動決定** | 人間に聞かないという原則の徹底 |
| 有料noteの前提 | 実績が必要 | **`productType` で分岐** | 実績なしでも道具型なら成立する |
| 校正・レイアウト | LLM スキル2本 | **Python の判定器** | 決定論で判定できるものをコードに移す |
| note への投稿 | Claude in Chrome | **HTML コピペ** | 重い・落ちる・画像トークンを食う |
| リポジトリ | Public + Pages（本文も追跡） | **Public のまま有料部分を除外** | 有料note本文が公開される穴があった |
| スキル数 | 26本 | **15本** | 削減の内訳は §3 |
| 月次コスト | 約4,780円相当（週1本） | **約1,840円相当（週7本）** | §9 |

**捨てないもの:** `sales-letter` / `letter-audit` / `ethics-line` と `critic` / `auditor` の設計思想は v2.0 のまま維持する。品質ゲートの構造こそがこのシステムの中核だからである。

---

## 1. 設計の主軸

### 1.1 何を解決するのか

副業でコンテンツ販売が続かない原因は、ネタ探し・構成・執筆・校正・レイアウト・告知・導線を**1人の頭で同時並行している**ことにある。続けられるかどうかを、意志力の問題から設計の問題に変える。

**人間に残すのは以下の2つだけとする。**

| 人間がやること | 機械がやること |
|---|---|
| プレビュー画面から本文をコピーして note に貼る | 調査・リサーチ・分析・テーマ決定・記事生成のすべて |
| ダッシュボードで管理する | 数値取得・診断・打ち手の提示 |

### 1.2 なぜ二層モデルなのか

v2.0 は3部署（リサーチ部・編集部・営業部）で分けていた。v3.0 は**周期**で分ける。

理由は単純で、**リサーチを日次で回すとトークンが尽きるから**である。日次リサーチは1回あたり約66円、月30回で約2,000円。Pro プラン（$20 ≈ 3,000円）の3分の2をリサーチだけで使い切る計算になる。

したがって:

- **週次リサーチ層** — 月曜に1回だけ web 検索を行い、7本のテーマを仕込む
- **日次生成層** — 在庫から1本引くだけ。**web 検索をしない**
- **決定論ゲート層** — 文体・重複・note記法を GitHub Actions で機械判定する。**トークン0**

日次生成が web 検索をしないことが、毎日投稿を成立させている唯一の理由である。

### 1.3 なぜ「セールスレター」が中核であり続けるのか

有料noteの無料部分は、そのままセールスレターである。レターの役割は3つに還元できる。

> ①開かせる ②欲しくさせる ③買わない理由を潰す

これが中核である理由は v2.0 から変わらない。

**理由1: 完全自動化の成否がここで決まる。** 人が最終チェックをしないなら、最も売上に直結する部分を機械が自己診断できなければならない。レターは診断項目を明確に言語化できる唯一の工程である。

**理由2: 判断基準を持たせやすい。** 「理想の未来と再現性が交互に来ているか」「買わない理由6つを潰したか」は×判定が可能で、「生成だけのスキルを作らない」という原則と合致する。

### 1.4 完全自動化を成立させる安全弁

人が見ないぶん、機械が自分を落とす仕組みが必要になる。v3.0 では**安全弁を2種類に分ける。**

| 種別 | 実装 | コスト | 担当 |
|---|---|---|---|
| **決定論ゲート** | Python スクリプト | **トークン0** | 文体・表記・note記法・重複 |
| **判断ゲート** | サブエージェント | 有料 | 離脱・購買・法的線引き |

**決定論で判定できるものを LLM に考えさせない。** これが v3.0 のコスト設計の中核である。「すべてのスキルは物差しを持つ」という原則は維持したまま、物差しの実装を移す。

---

## 2. 二層モデルと Claude Code 機能の割り当て

### 2.1 全体構造

| 層 | 役割 | 使う機能 | 実行場所 | 頻度 |
|---|---|---|---|---|
| 週次リサーチ | 1週間分のテーマを仕込む | Skills + Web検索 | **ローカル** | 日曜 |
| 週次生成 | 在庫から7本引いて記事にする | Skills + Subagents | **ローカル** | 日曜 |
| 決定論ゲート | 文体・重複・記法を機械判定 | GitHub Actions + Python | GitHub | push のたび |
| 受け渡し | HTML にして人間に渡す | Node + localhost | ローカル | 週1回 |
| 経営 | 投稿と管理 | プレビュー / ダッシュボード | ブラウザ | 随時 |

### 2.2 Routines の制約（v2.0 から変更なし）

Routines は保存した設定を Anthropic のクラウド上で自動実行する仕組みで、ローカルPCの起動を必要としない。

| 制約 | 設計への影響 |
|---|---|
| 日次実行上限（Pro 5回/日） | Routine を3本に絞る |
| 承認なしで実行される | プロンプトは自己完結形式で書く。「前回の続き」は機能しない |
| research preview 段階 | 退避先（GitHub Actions + ローカル `/loop`）を確保 |
| **ブラウザ操作ができない** | **v3.0 では影響しない。ブラウザ操作そのものを廃止したため** |

### 2.3 Routines の配分（Pro = 5回/日）

| # | Routine | トリガー | 頻度 | 消費 |
|---|---|---|---|---|
| 1 | `/note-week` | 日曜（`market-research` の21時以降）・**ローカル** | 週1回 | — |
| 2 | `weekly-research` | 月曜 07:00 | 週次 | 1（月曜のみ） |
| 3 | `monthly-review` | 月初 08:00 | 月次 | 1（月初のみ） |

**最大消費は「月初が月曜と重なった日」の3回。** Pro の5回/日に収まり、予備2枠が手動実行用に残る。

**`daily-build` が木・日に動かない理由:** 有料部分の本文（`03-draft.md`）を追跡しないため、クラウドで生成すると手元に残らない（§10.2）。有料noteはローカルで作る。

**日次生成を1本のルーチンに統合できる理由:** `themes.md` の各テーマが `type`（free / paid）と曜日を持つため、ルーチンは在庫から今日の1本を引くだけでよい。

### 2.4 GitHub Actions の役割

**Actions で記事生成はしない。** Claude Code Action は Anthropic API キーによる従量課金でサブスク枠外になる。Actions が担当するのは**トークンを一切消費しない処理**だけである。

| ワークフロー | トリガー | 中身 |
|---|---|---|
| `lint-and-dedup` | `notes/**` への push | `lint.py` + `dedup.py`。両方走らせてから判定し、詳細を Job Summary に出す |
| **`market-research`** | **日曜 21:00 JST** | **`note_market.py`。note の公開データから需要と供給を実測し `research/market/` を更新する。翌朝の `weekly-research` はこの要約表を読む** |
| `fetch-metrics` | 毎朝 06:30 | note 公開APIから数値取得 → 暗号化 → `data/` 更新 |
| `stock-alert` | 毎朝 07:00 | `themes.md` の未使用が3本未満なら Issue |
| `weekly-digest` | 日曜 20:00 | 週次サマリーを Issue 化 |
| `deploy-pages` | `preview/public/**` `data/**` `demo/**` への push | ダッシュボードと投稿キューと**デモ**を Pages へ。**暗号化はせず配るだけ**（§7.3） |

**`market-research` はこの原則に抵触しない。** やるのは生成ではなく計測である。
集計もクラスタリングも Python で完結し、LLM を呼ばない。

public repo なので実行時間は無制限。

**公開版プレビューの生成を Actions に置かない。** 暗号化には `03-draft.md` が要るが、それは `.gitignore` 対象で Actions 上に存在しない。ローカルで暗号化し、`preview/public/` をコミットして持ち込む（§7.3）。

---

## 3. スキル設計（全15本）

v2.0 の26本から14本を削減する。

### 3.1 削減の内訳

| 分類 | スキル | 措置 | 理由 |
|---|---|---|---|
| 統合 | `daily-trend-scan` / `pain-mining` / `competitor-teardown` | → `weekly-research` | 日次リサーチはトークンが持たない |
| 統合 | `content-outline` | → `product-concept` に統合 | 工程を分ける利得がない |
| コードへ移管 | `proofreading` | → `scripts/lint.py` | 決定論で判定できる |
| コードへ移管 | `note-layout` | → `scripts/lint.py` | 同上 |
| 廃止 | `profile-setup` | → `account-research` + `account-design` + `profile-accumulator` | 人間に聞かない |
| 廃止 | `image-prompt` | — | タイトル・カテゴリ・本文だけで足りる |
| 廃止 | `note-draft-push` | → HTML コピペ | 重い・落ちる・画像トークンを食う |
| 凍結 | 営業部 `S1`〜`S5` | — | 実績が貯まるまで着手しない |

### 3.2 リサーチ層（4本）

| # | スキル | 判断基準 | 出力 |
|---|---|---|---|
| R1 | `account-research` | note で伸びているアカウントを解剖。**フォロワー数では評価しない。** スキ率・投稿頻度・参入時期・有料noteの型で見る | `research/accounts/{genre}.md` |
| R2 | `account-design` | 主カテゴリ1つ＋サブ2〜3つを確定。ペルソナと投稿方針も決める。**人間に聞かない** | `knowledge/account.md` |
| R3 | `weekly-research` | 週1で7本のテーマを `type` と曜日付きで仕込む。**カテゴリ範囲外は在庫に入れない** | `research/themes.md` |
| R4 | `pricing-strategy` | 情報密度と価格の対応。**天井を下げる判断**を含む（届かない100万より届く5万） | 価格 + 根拠 |

#### R1 `account-research` の評価軸

フォロワー数は参入時期との交絡があるため単独では使えない。以下で評価する。

| 軸 | 見るもの |
|---|---|
| スキ率 | スキ数 ÷ 推定PV。アカウントの質を最も正直に表す |
| 投稿頻度 | 継続できている型かどうか |
| 参入時期 | 先行者利益なのか、いま参入しても勝てるのか |
| **有料noteの型** | **`tool` か `experience` か。実績なしで参入できるジャンルかを判定する** |
| 無料/有料の境界 | どこで切っているか |
| **あえて書いていない領域** | 空白がそのまま自分の場所になる |

#### R3 `weekly-research` の在庫設計

`themes.md` の各エントリは以下を持つ。

```yaml
- id: 2026-W32-01
  title: テーマの一行要約
  type: free            # free | paid
  day: mon              # mon..sun
  category: 主カテゴリ / サブカテゴリ
  score: { strength: 4, market: 3, willingness: 2 }
  rationale: なぜこれを選んだか。次点は何で、なぜ落としたか
  sources: [URL, URL]
  status: pending       # pending | used | rejected
```

**在庫が切れたら止まる。** 空の在庫を埋めるためにその場でテーマを作らない。`stock-alert` が3本未満で Issue を立てる。

### 3.3 生成層（9本）

| # | スキル | 判断基準 | 出力 |
|---|---|---|---|
| E1 | `daily-article` | 在庫から1本引いて**2500〜4000字。実物（プロンプト全文／コード全文／before-after）を必ず1つ持たせる** | `01-draft.md` / `meta.json` |
| E2 | `product-concept` | 強み × 競合の穴 × 深層心理 の交点を1文で。**`productType` を確定する**。構成もここで決める | `00-concept.md` |
| E3 | **`sales-letter`** | 無料部分＝セールスレター。**理想の未来と再現性を交互に配置**し、最後は理想の未来で終える。恐怖は1つだけ | `02-letter.md` / `letter-meta.json` |
| E4 | `draft-writing` | 有料部分。抽象論禁止。道具型では**動作条件と最初の1歩**を必ず書く | `03-draft.md` |
| E5 | **`letter-audit`** | 16項目を○×判定。**×が残れば E3 に差し戻し（最大2周）** | `04-audit.md` / `audit.json` |
| E6 | **`ethics-line`** | 偽の限定・盛った恐怖・裏付けのない体験談を検出。**有料note必須／無料は `lint.py` の検出時のみ** | `ethics.json` |
| E7 | `reader-feedback` | ペルソナになりきって読み、離脱ポイントを検出（**週1本だけ**） | `05-feedback.md` |
| E8 | `title-design` | 5タイプ×8案を生成して採点 | `07-titles.md` |
| E9 | `build-report` | 今回の生成で何を判断したかのログを1枚に | `report.md` |

> 上表は9項目あるが、`product-concept`〜`build-report` のうち有料note専用のものは日次では起動しない。日次で動くのは E1 / E6 / E7 / E9 の4本である。

#### E3 `sales-letter` の内部構造

```
1.  フック          読者が「自分のことだ」と感じる一文
2.  問題の可視化    行動で名指しする（状態ではなく）
                    ✕「テーマが決まってない人」
                    ○「エディタを開いて、3日書けてない人」
3.  理想の未来 ①
4.  再現性 ①        単純化（やることを1つに絞る）
5.  理想の未来 ②
6.  再現性 ②        簡易化（時間と手間を数字で出す）
7.  恐怖            1つだけ。回避できる位置で止める（「もう手遅れ」にしない）
8.  理想の未来 ③
9.  再現性 ③        ★型で分岐
10. 再現性 ④        ★型で分岐
11. 買わない理由の除去（6つ）
12. 漏斗            断定 → 外の選択肢を消す → 最後の行動は1つだけ
13. 理想の未来で締める
```

**★ 再現性③④の型分岐（v3.0 の中核変更）**

| ブロック | `type: experience` | `type: tool` |
|---|---|---|
| 再現性③ | 天井を下げる ＋ 自分以外の結果を1つ | **成果物の中身を具体的に開示する ＋ 動作条件を明記する** |
| 再現性④ | 共感（自分がダメだった頃の話） | **この道具がなかったときに何が面倒だったか** |

道具型は再現性の根拠を「書き手の過去」ではなく「成果物の検証可能性」に置く。買った人が手元でコピペして動かせるかどうかが証明であり、書き手が何者かは関係ない。

#### E5 `letter-audit` の判定16項目

```
① 欲しくさせる側
  □ 理想の未来と再現性が交互に来ているか
  □ 最後が理想の未来で終わっているか
  □ 再現性が4つで作られているか
  □ 約束が「何でもできる」になっていないか
② 恐怖
  □ 恐怖は1つだけか
  □ 「もう手遅れ」になっていないか
  □ 何を地獄に置いたか。それで集まる客層は狙い通りか
③ 買わない理由6つ
  □ そもそも問題があると思ってない → 行動で名指ししたか
  □ 再現性がなさそう           → ★型で分岐
  □ 今買う理由がない           → 本物の期限・値上げ・特典終了を置いたか
  □ 高い、あとでいい           → 中身を分解したか（値下げで潰していないか）
  □ 自分には早い               → 誰向けか、向かない人も書いたか
  □ 本当に結果が出るのか       → どこまでやれば結果が出るか書いたか
④ 漏斗
  □ 断定しているか
  □ 外の選択肢に触れて消したか
  □ 最後に提示する行動は1つだけか
```

**★ C2「再現性がなさそう」の型分岐**

| 型 | ×になる条件 |
|---|---|
| `experience` | 天井を下げていない、または自分以外の結果が1つも無い |
| `tool` | 成果物の中身が開示されていない / 買った人が最初の1歩を踏めない / 動作条件・前提環境が書かれていない |

**判定は真偽値で返す。** 曖昧な評価コメントではなく、○×と根拠箇所（行番号）を返させる。×が1つでも残れば差し戻す。**上限は2周**（v2.0 の3周から短縮）。

#### E6 `ethics-line` のモード分岐

| モード | 対象 | 検出項目 |
|---|---|---|
| `full` | 有料note | E1〜E5 の全5項目 |
| `light` | **無料記事** | **E3（裏付けのない実績）と E5（断定表現）のみ** |

無料記事は売らないため E1（偽の限定）と E4（書きっぱなしの弱点）が構造的に発生しない。一方 E3 と E5 は実績のないアカウントほど混入しやすいため、絶対に外さない。

**どちらのモードでも差し戻し回数に上限を設けない。** 景表法・特商法に関わるため。

### 3.4 経営層（2本）

| # | スキル | 役割 |
|---|---|---|
| C1 | `profile-accumulator` | 週次で運用ログと `data/` の数値から「言える実績」を `profile.md` に追記する。**出所は必ず `data/` か `report.md`。創作しない** |
| C2 | `revenue-maximizer` | 顧客数 × 単価 × 購入回数 のボトルネック診断 |

**C1 が v3.0 の要である。** 実績のない状態から始めて、運用そのものを実績に変換する。3ヶ月後には `type: experience` の有料noteを書ける材料が揃う。

---

## 4. 決定論ゲート（Python）

### 4.1 `scripts/lint.py`

**依存なし（標準ライブラリのみ）。** LLM を使わない。

当初は sudachipy による形態素解析を想定していたが、実際に必要な判定を洗い出すと
文分割は `[。！？]` の正規表現で足り、漢字比率は Unicode 範囲を数えるだけで済んだ。
依存ゼロなら Actions が `pip install` なしで走り、起動が速く壊れにくい。

| 検出項目 | 基準 |
|---|---|
| 一文の長さ | 60文字超で警告、100文字超でエラー |
| 漢字かな比率 | 漢字が40%を超えたら警告（読みにくい） |
| 表記ゆれ | 同一記事内で「ですます／である」混在、送り仮名の不統一 |
| 禁止表現 | `voice.md` の `ng` ブロックに列挙されたもの |
| 見出し階層 | H1 の重複、H2 を飛ばした H3 |
| **note で崩れる記法** | **テーブル・脚注・3階層以上のリスト** |

### 4.2 `scripts/dedup.py`

**依存なし（標準ライブラリのみ）。文字3-gram の TF-IDF による類似度判定。**

日本語は分かち書きが必要なため単語 TF-IDF は形態素解析に依存する。代わりに文字3-gram を使う。
日本語の短文類似度では文字 N-gram のほうが安定し、scikit-learn も不要になる。

比較前に**コードブロックを除去する。** 同じライブラリを扱う記事はコードが似るため、
残すと無関係な記事まで高い類似度になる。

| 類似度 | 措置 |
|---|---|
| 0.85 超 | **ブロック**。Actions を失敗させ Issue を立てる |
| 0.70〜0.85 | 警告。`report.md` に記録して通す |
| 0.70 未満 | 通す |

**毎日投稿で最も現実的な事故はネタの反復である。** 3週間で同じことを言い始める。これを LLM で判定すると毎回トークンを食うが、コードなら実行時間数秒・トークン0で済む。

---

## 5. サブエージェント設計（5体）

**スキル = 判断基準の定義、サブエージェント = 独立コンテキストでの実行。**

| エージェント | モデル | 使う場面 | 分離する理由 |
|---|---|---|---|
| `researcher` | Sonnet 5 | R1〜R3 | Web検索の結果でコンテキストが汚れる。要約だけを返させる。**スコアリングも兼務する** |
| `writer` | Sonnet 5 | E1, E4 | 本文の執筆に集中 |
| `letter-writer` | Sonnet 5 | E3 | レター専用。構造テンプレートだけを持たせる |
| `critic` | **Opus 5** | E5, E7 | **親のコンテキストを見せない。** 自分が書いた文章だと知っている状態では正直な×が出せない |
| `auditor` | **Opus 5** | E6 | 線引きの検出専用。他の文脈を一切持たせない |

**v2.0 の `analyst` を廃止し `researcher` に統合した。** スコアリングはリサーチ直後に行うためコンテキスト分離の必然性が薄く、データを渡し直すコストのほうが大きい。

**`critic` と `auditor` の分離は維持する。** critic は「売れるか」を見る。auditor は「やってはいけないことをしていないか」を見る。同じエージェントに両方やらせると、売るための表現を守ろうとして線引きが甘くなる。

---

## 6. 自動フローの全体像

### 6.1 週次リサーチ（月曜 07:00）

```
[月 07:00] weekly-research 起動（Sonnet 5 / effort: high）
  ↓
 knowledge/account.md からカテゴリ範囲を読む
  ↓
 researcher がweb検索15件 + 競合3本を解剖 + 読者の悩みを収集
  ↓
 スコアリング（強み × 市場性 × 支払い意欲）
  ↓
 カテゴリ範囲外を除外し learnings.md に理由付きで記録
  ↓
 research/themes.md に7本を投入（type と曜日を割り当て）
  ↓
 git commit & push
```

### 6.2 日次生成

**無料6本（月〜土）と有料note 1本（日）を、日曜に `/note-week` がローカルで一括生成する。**

```
┌─ 無料記事 ── [日曜] /note-week が writer を6つ並列起動（ローカル）─┐
│                                                                     │
│  themes.md から今日の曜日のテーマを1本引く                          │
│    ↓                                                                │
│  daily-article（writer / Sonnet 5 / medium）→ 01-draft.md, meta.json│
│    2500〜4000字＋実物（プロンプト／コード／before-after）を1つ以上   │
│    ↓                                                                │
│  lint.py / dedup.py で一次判定（トークン0）                         │
│    ├ no-artifact が error → 実物を足す                              │
│    └ unverified-claim を検出 → needsAudit: true                     │
│    ↓                                                                │
│  needsAudit のときだけ ethics-line（auditor / light）上限なし       │
│    検出が無ければ auditor を呼ばない                                │
│                                                                     │
│  ※ reader-feedback（critic）は週1本だけ。日曜にサンプリングする    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─ 有料note ── [木・日] ローカルで「お願いします」──────────────────┐
│                                                                     │
│  themes.md から今日の曜日のテーマを1本引く                          │
│    ↓                                                                │
│  product-concept → productType を確定 → 00-concept.md               │
│    ↓                                                                │
│  sales-letter（letter-writer）→ 02-letter.md（追跡する）            │
│  draft-writing（writer）→ 03-draft.md（★追跡しない）                │
│    ↓                                                                │
│  letter-audit（critic / Opus 5）最大2周                             │
│  ethics-line（auditor / full）上限なし                              │
│    ↓                                                                │
│  title-design（8案）                                                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
  ↓
 build-report → report.md
  ↓
 themes.md の該当エントリを status: used に更新
  ↓
 git commit & push（03-draft.md は .gitignore で除外される）
  ↓
[GitHub Actions] lint-and-dedup が走る（トークン0）
  → 0.85超の重複ならブロックして Issue
  → 文体・note記法の違反を検出
  ↓
[人間] 週1回 /note-preview を実行 → ローカルで HTML 生成 → localhost:5173
  → 「本文をコピー」→ note に貼る → 公開
```

**`lint.py` は `03-draft.md` を検査できない。** Actions 上には存在しないためである。有料部分の文体チェックは `/note-preview` の実行時にローカルで走らせる。

**人間の関与はこの図の中で1箇所だけ。** コピーして貼ること。

### 6.3 月次（月初 08:00）

```
[月初 08:00] monthly-review
  → revenue-maximizer が metrics.json と revenue.json を読む
  → ボトルネック診断
  → profile-accumulator が「言える実績」を profile.md に追記
  → ダッシュボードの「今月の打ち手」に反映
```

---

## 7. 投稿の受け渡し

### 7.1 なぜブラウザ自動操作を廃止したか

| 理由 | 内容 |
|---|---|
| 重い・落ちる | note のエディタは ProseMirror ベースの独自実装。スクリーンショット→解析→クリックの往復が長い |
| **トークンを食う** | **スクリーンショットは画像トークン（1枚最大4,784）。1本10〜20枚で20〜40k tokens。週7本なら月600k〜1.2M tokens** |
| 人間の時間もむしろ増える | 自動操作の完了を待つより、HTML からコピペするほうが速い（1本30秒） |

### 7.2 HTML コピペ方式

```
notes/{slug}/02-final.md + meta.json
  ↓ scripts/build-preview.mjs（Markdown → note互換HTML）
preview/{slug}.html
  ↓ /note-preview → scripts/serve.mjs → localhost:5173
[人間] コピーボタン → note に貼る
```

| 画面 | 内容 |
|---|---|
| `preview/index.html` | 投稿待ち一覧。タイトル・カテゴリ・type・文字数・公開予定日。投稿済みチェック（localStorage） |
| `preview/{slug}.html` | note のエディタに近い見た目でレンダリング。コピーボタン群 |

**コピーボタン**
- タイトルをコピー
- 本文をコピー（無料記事）
- **無料部分をコピー / 有料部分をコピー**（有料note。note は境界線を手で設定するため分割が必要）
- ハッシュタグをコピー

### 7.3 有料部分の暗号化（スマホ対応）

公開ページの有料部分は **AES-GCM-256** で暗号化する。ダッシュボードと同じ形式である。

```
base64( salt[16] || iv[12] || ciphertext+tag )
PBKDF2 / SHA-256 / 310,000回 / 鍵32バイト
```

| 出力 | 場所 | 有料部分 | 追跡 | どこから |
|---|---|---|:---:|---|
| ローカル | `preview/` | 平文 | ✕ | PC |
| 公開 | `preview/public/` | 暗号化 | ○ | **スマホ** |

**暗号化はローカルでしか行えない。** `03-draft.md` は `.gitignore` 対象で GitHub Actions 上に
存在しないため、そこでは暗号化すらできない。生成物をコミットして持ち込み、
`deploy-pages` は配るだけにする。

**実装上の注意:** Node の `crypto` は認証タグを `getAuthTag()` で別に返す。
Web Crypto API は ciphertext の末尾に含む仕様なので、連結しないと復号できない。

合言葉は `secrets/passphrase.txt` または環境変数 `DASHBOARD_PASSPHRASE`。
`crypto-util.mjs` が8文字未満を拒否し、15文字未満で警告する。
**暗号文が公開されるため、短いと辞書攻撃で破られる。**

復号した合言葉は `localStorage` に持つ。**一度入れれば次回以降は自動で開く。**
スマホから毎回入力するのは現実的でないため、利便性を取った。

そのぶん端末に残るので、**「合言葉を消す」ボタンを必ず用意する。**
保存済みの合言葉で失敗したら自動で削除し、入力画面に戻す。

### 7.4 投稿済みの自動除外

公開版は投稿済みを載せない。note の記事と同じ内容が2箇所に残らないようにするためである。

| 判定 | 条件 |
|---|---|
| 明示 | `meta.json` の `posted: true` |
| 自動 | `publishDate` が今日より前 |

静的サイトからサーバーに書き戻せないため、日付を唯一の自動判定材料にしている。

### 7.5 配信直前の検査

`deploy-pages` は以下を検査し、1つでも該当したら**公開せずに失敗する。**

1. Markdown が配信物に含まれていないか
2. 合言葉の記述が残っていないか
3. 有料部分が平文でないか（`class="paywall"` の検出）
4. 有料記事に暗号文（`data-enc`）があるか
5. 全ページに `noindex` があるか
6. `data/` に平文とみられる売上データが無いか

### 7.6 書式が保持される仕組み

クリップボードに `text/html` と `text/plain` を同時に書き込む。note のエディタは `text/html` を読むため、**見出し・太字・リスト・引用・区切り線がそのまま入る。**

```js
await navigator.clipboard.write([
  new ClipboardItem({
    'text/html':  new Blob([html], { type: 'text/html' }),
    'text/plain': new Blob([text], { type: 'text/plain' }),
  }),
]);
```

**`file://` では動かない。** `navigator.clipboard.write()` はセキュアコンテキストを要求する。HTMLファイルをダブルクリックで開くと使えないため、`serve.mjs` が `http://localhost:5173` で配信する。

### 7.7 note で崩れる記法

| 記法 | note | 対応 |
|---|---|---|
| **テーブル** | **非対応** | 箇条書きに変換。変換不能なら `lint.py` が警告 |
| 脚注 | 非対応 | 本文に展開 |
| 3階層以上のリスト | 崩れる | 2階層までに矯正 |
| コードブロックの言語指定 | 保持されない | 警告のみ（貼付後に手動指定） |
| 水平線 `---` | 区切り線になる | 使ってよい |

---

## 8. ディレクトリ構成

```
note-factory/
├── CLAUDE.md                     # 全体ルール・禁止事項・文体定義
├── ARCHITECTURE.md
├── README.md
├── note-factory-spec.md          # 本ファイル
├── .gitignore
│
├── .claude/
│   ├── skills/
│   │   ├── research/             # account-research / account-design / weekly-research / pricing-strategy
│   │   ├── editorial/            # daily-article / product-concept / sales-letter / draft-writing
│   │   │                         # letter-audit / ethics-line / reader-feedback / title-design / build-report
│   │   └── exec/                 # profile-accumulator / revenue-maximizer
│   ├── agents/                   # researcher / writer / letter-writer / critic / auditor
│   └── commands/                 # note-init / note-preview / note-revise / note-letter / note-review
│
├── routines/
│   ├── daily-build.md
│   ├── weekly-research.md
│   └── monthly-review.md
│
├── knowledge/
│   ├── account.md                # カテゴリとペルソナ。全記事の範囲を決める
│   ├── profile.md                # 実績。profile-accumulator が自動追記。当面は空
│   ├── voice.md                  # 文体・禁止表現。lint.py が参照
│   ├── products.md
│   └── learnings.md              # 却下理由・範囲外テーマ
│
├── research/
│   ├── accounts/{genre}.md
│   ├── market/                   # ★ 需要と供給の実測（note_market.py が生成）
│   │   ├── hashtags.txt          #   測るタグ。カテゴリを変えたらここも変える
│   │   ├── {date}.md             #   要約表。weekly-research が読むのはこれだけ
│   │   ├── {date}.json           #   数値の全量
│   │   └── raw/                  #   生キャッシュ（.gitignore 対象）
│   ├── themes.md                 # ネタ在庫
│   └── trends/{date}.md
│
├── notes/{slug}/                 # §記事単位の成果物
│   └── demo/                     # ★ before.html / after.html
├── demo/{slug}/                  # ★ build-demo.mjs の出力。Pages で配信。追跡する
├── preview/                      # build-preview.mjs の出力（.gitignore 対象）
├── data/                         # metrics.json / history.jsonl / revenue.json
├── scripts/
│   ├── build-preview.mjs
│   ├── build-demo.mjs            # ★ before/after の並置ビュー
│   ├── serve.mjs
│   ├── fetch-public.mjs
│   ├── encrypt.mjs
│   ├── lint.py                   # 依存なし（標準ライブラリのみ）
│   └── dedup.py                  # 依存なし（標準ライブラリのみ）
├── secrets/                      # passphrase.txt（.gitignore 対象）
└── .github/workflows/            # 5本
```

### 記事単位の成果物

| ファイル | 無料 | 有料 | 中身 |
|---|:---:|:---:|---|
| `00-theme.md` | ○ | ○ | 引いたテーマと選定理由 |
| `01-draft.md` | ○ | — | 本文 |
| `02-final.md` | ○ | ○ | 修正済みの完成版 |
| `meta.json` | ○ | ○ | タイトル / カテゴリ / ハッシュタグ / type / 公開予定日 |
| `report.md` | ○ | ○ | 判断ログ |
| `00-concept.md` | — | ○ | `productType` を含むコンセプトと構成 |
| `02-letter.md` | — | ○ | 無料部分（セールスレター）。追跡してよい |
| `letter-meta.json` | — | ○ | レターの構造メタ |
| **`03-draft.md`** | — | ○ | **有料部分。`.gitignore` 対象。ローカルにのみ存在する** |
| `audit.json` | — | ○ | letter-audit の判定 |
| `ethics.json` | ○ | ○ | ethics-line の検出 |

---

## 9. コストとトークン試算

### 9.1 単価（2026-08-05 時点）

| モデル | 入力 $/MTok | 出力 $/MTok | 備考 |
|---|---:|---:|---|
| Claude Sonnet 5 | $3.00 | $15.00 | 2026-08-31 まで導入価格 $2.00 / $10.00 |
| Claude Opus 5 | $5.00 | $25.00 | |

Sonnet 5 は新トークナイザで、同じ日本語が旧モデル比で**約30%多いトークン**になる。以下の試算はこれを織り込んでいる。

### 9.2 工程別の消費

**無料記事1本**

> **2026-08-07 の改訂でこの表は変わった。** レビューを削り、テーマ選定に投資した。
> 記事が長くなった（実物を必須化した）ぶん執筆は増えるが、レビュー側が大きく減る。

| 工程 | 入力 | 出力 | モデル |
|---|---:|---:|---|
| 執筆 | 12k | 9k | Sonnet 5 (medium)。**実物ぶん増えた** |
| 校正・記法・実物の有無・実績主張 | — | — | **lint.py（トークン0）** |
| 重複判定 | — | — | **dedup.py（トークン0）** |
| `reader-feedback` | 6k | 1k | Sonnet 5。**週1本だけ**（5本に按分すると 1.2k / 0.2k） |
| `ethics-line`（light） | 6k | 1k | Opus 5。**`lint.py` が検出したときだけ** |
| 修正1周 | 8k | 3k | Sonnet 5 |
| **計（検出なしの通常時）** | **約21k** | **約12k** | 執筆が主。Opus は呼ばれない |

**週5本ぶんで見ると、Opus の呼び出しが週5回からほぼ0回になる。**
一方 `note_market.py`（市場計測）は Python なので**トークン0**である。

| 施策 | 変化 |
|---|---|
| `reader-feedback` 週5 → 週1 | critic 呼び出し −4/週 |
| 無料記事の `ethics-line` を検出時のみに | Opus 週5 → ほぼ0 |
| `weekly-research` の web 検索 15件 → 5件 | 週次の最大コスト源が 1/3 |
| `note_market.py` を追加 | **±0**（Actions・LLM を使わない） |

**有料note 1本（軽量版）**

| 工程 | 入力 | 出力 | モデル |
|---|---:|---:|---|
| concept + 構成 | 18k | 5k | Sonnet 5 |
| sales-letter | 25k | 8k | Sonnet 5 |
| draft-writing | 30k | 15k | Sonnet 5 |
| letter-audit | 20k | 3k | **Opus 5** |
| ethics-line（full） | 18k | 3k | **Opus 5** |
| 差し戻し2周 | 35k | 10k | Sonnet 5 |
| title-design + report | 15k | 4k | Sonnet 5 |
| **計** | **161k** | **48k** | **約120円** |

**週次リサーチ1回**: 100k in / 18k out ≈ 57円

### 9.3 月次合計

| 項目 | 単価 | 頻度 | 月額 |
|---|---:|---|---:|
| 無料記事 | 21円 | 週5本 | 約450円 |
| 有料note | 120円 | 週2本 | 約1,040円 |
| 週次リサーチ | 57円 | 週1回 | 約250円 |
| 月次レビュー | 100円 | 月1回 | 100円 |
| **合計** | | **月30本** | **約1,840円** |

Pro（$20 ≈ 3,000円/月）に対して**安全率1.6倍**。

**ただしこれは API 換算である。** Claude Code のサブスクは定額なので実質的には有利だが、対話でコンテキストが膨らむぶん実消費は増える。**ステップ4で `/cost` の実測を必ず取り、想定の2倍を超えるなら `effort` とスキル定義の長さを見直す。**

### 9.4 v2.0 との比較

| | v2.0 | v3.0 |
|---|---:|---:|
| 月間投稿数 | 4本 | **30本** |
| 月額（API換算） | 約4,780円 | **約1,840円** |
| 1本あたり | 約1,195円 | **約61円** |

削減の内訳:
1. リサーチを日次→週次（-1,750円/月）
2. 競合解剖を記事生成時から週次へ移動
3. 校正・レイアウトを `lint.py` へ（LLM不要）
4. ブラウザ自動操作の廃止（画像トークンの消滅）
5. `image-prompt` の廃止
6. 差し戻し上限 3周→2周
7. Sonnet 5 主体化（Opus 5 はゲートのみ）

### 9.5 インフラ費用

| 項目 | 使うもの | 費用 |
|---|---|---|
| 実行環境 | Claude Code Pro | $20/月 |
| Routines / Dynamic Workflows | Claude Code に含まれる | ¥0 |
| リポジトリ / 定期実行 | GitHub Public + Actions（**実行時間無制限**） | ¥0 |
| ダッシュボード配信 | GitHub Pages（暗号化データ） | ¥0 |
| プレビュー配信 | localhost:5173（ローカルのみ） | ¥0 |
| 見出し画像 | note のデフォルト機能 | ¥0 |

---

## 10. リポジトリの公開設定

リポジトリ: https://github.com/kou56250046-cloud/note-automation-app（**public**）

public にする理由は、GitHub Pages と Actions の無料枠を使えるからである。

| 項目 | public | private |
|---|---|---|
| Actions 実行時間 | **無制限** | 月2,000分無料 |
| GitHub Pages | **無料** | Pro（$4/月）が必要 |
| 有料note本文 | **追跡してはならない** | 追跡してよい |

### 10.1 リポジトリ名では守れない

v2.0 の未決事項6には「検索で発見されにくいリポジトリ名」とあったが、これは対策にならない。URL を知れば誰でも読めるうえ、GitHub の検索にもかかる。**リポジトリ名で守るのではなく、ファイル単位で除外する。**

| 対象 | 追跡 | 理由 |
|---|:---:|---|
| `notes/**/03-draft.md` | ✕ | **有料部分の本文。これが漏れたら商品にならない** |
| `preview/` | ✕ | 生成HTMLに有料部分が含まれる |
| `secrets/` | ✕ | 合言葉 |
| `notes/**/02-letter.md` | ○ | note 上でも無料公開する部分 |
| `data/` | ○ | 暗号化済み |

### 10.2 この制約が決める2つのこと

**1. 有料noteの生成はローカル実行のみになる。**

クラウドで動くものはコミットして完了するため、`.gitignore` されたファイルを手元に残せない。したがって **Routines を廃止し、生成をすべてローカルに移した。** `/note-week` が日曜に7本まとめて作る。

副次的に Routines の消費が週7回から週5回に減り、Pro の枠にさらに余裕が出る。

**2. 公開版プレビューの生成もローカルのみになる。**

`03-draft.md` が Actions 上に存在しないため、そこでは暗号化すらできない。ローカルで暗号化し、`preview/public/` をコミットして持ち込む（§7.3）。

これはクリップボードAPIのセキュアコンテキスト要件とも一致するため、どのみち localhost が必要だった。

### 10.3 ダッシュボードは Pages で配信する

`data/` は暗号化済みなので GitHub Pages で公開してよい。復号は合言葉で行う。スマホから数値を確認できる。

**プレビュー（記事本文）と数値ダッシュボードで扱いを分ける**のが要点である。

---

## 11. コマンド

| コマンド | 動作 | 実行場所 |
|---|---|---|
| `/note-init` | 初回のみ。`account-research` → `account-design`。**ヒアリングなし** | ローカル |
| **`お願いします`** | `daily-build` を即時実行（1本作る） | ローカル |
| **`/note-preview`** | `build-preview.mjs` → `localhost:5173` で投稿キューを開く。**週1回の運用作業** | ローカル |
| `/note-revise {slug} "指示"` | 完成品に差し戻し指示を出して再生成 | ローカル |
| `/note-letter {slug}` | 有料noteのレター診断ループを回す | ローカル |
| `/note-review` | `revenue-maximizer` を起動 | ローカル |

`/note-setup` と `/note-post` は廃止。

---

## 12. ダッシュボード仕様

デザイン方針（青焼き / Shippori Mincho B1 / 資産曲線）は v1.1 から変更なし。プロトタイプは実装済み。**GitHub Pages ではなく `localhost:5173` で開く。**

| タブ | 内容 | v3.0 での変更 |
|---|---|---|
| ホーム | KPI / 資産曲線 / フォロワー推移 | **投稿待ちの記事を最上部に** |
| 記事 | 一覧 / スキ率 / タイトル型別成績 | **カテゴリ別成績を追加**（ブランド化の効き目を見る） |
| 商品ライン | 価格の階段図 / 無料有料比率 | **type 別（tool / experience）の内訳を追加** |
| フロー | 二層モデルの稼働状況 | 3部署モデルから描き替え |
| **自動運転ログ** | Routine の実行結果、差し戻し回数、選定理由 | **`lint.py` / `dedup.py` の警告も表示** |
| 入力 | 月次売上の手入力 | 変更なし |

### 自動運転ログタブ

完全自動化の最大のリスクは「何が起きているか分からなくなること」である。以下を時系列で表示する。

- どの Routine がいつ動いたか、成功したか
- `weekly-research` が何を選び、なぜ選んだか（**却下ボタン付き**）
- 各ゲートで何回差し戻したか
- ゲートで検出された×の内容（特に `ethics-line` の検出は必ず目を通す）
- **`dedup.py` の類似度警告**（0.70〜0.85 の通過分）

**却下ボタン:** 選ばれたテーマが違うと感じたとき、ワンクリックで `themes.md` に却下フラグを立てる。次回のスコアリングがそれを学習する。人間の関与を「最小1クリック」に圧縮する仕組み。

---

## 13. 開発ロードマップ

| ステップ | 内容 | 完了条件 |
|---|---|---|
| **0** | `.gitignore` 整備（`03-draft.md` / `preview/` / `secrets/`）＋ remote 接続 | **有料note本文が追跡されない状態で push できる** |
| **1** | `account-research` + `account-design` + `/note-init` | `knowledge/account.md` にカテゴリとペルソナが生成される。**人間への質問がゼロ** |
| **2** | `weekly-research` + `themes.md` の在庫管理 | 月曜に7本が `type` と曜日付きで仕込まれる |
| **3** | `lint.py` + `dedup.py` + `lint-and-dedup` Actions | push すると LLM 抜きで品質判定が走る |
| **4** | `daily-article` + `ethics-line` light + `daily-build` | 「お願いします」で無料記事1本が完成する。**`/cost` で実測** |
| **5** | `build-preview.mjs` + `serve.mjs` + `/note-preview` | **HTML からコピペで note に投稿できる。毎日投稿が回り始める** |
| **6** | `sales-letter` / `letter-audit` の `productType` 対応 | `type: tool` で `/note-letter` が 16/16 通過する |
| **7** | `fetch-metrics` + `profile-accumulator` + ダッシュボード | 数値が自動取得され、`profile.md` に実績が積み上がる |
| **8** | `type: experience` の解禁 + 営業部 + 自動運転ログ | `profile.md` が育った段階で体験談型を解禁 |

**ステップ3を4より先にする理由:** 品質ゲートが動く前に毎日生成を始めると、重複記事とトーンのぶれた記事が初期30本として積み上がる。**初期30本がアカウントの初期評価をほぼ決める。** v2.0 が「安全弁を先に作る」としたのと同じ論理で、順番を入れ替えてはいけない。

**ステップ5を6より先にする理由:** 記事を作れても投稿できなければ運用が始まらない。無料記事だけでも先に投稿ルートを開通させ、実際の反応を見ながら有料noteに進む。

### ウォームアップ期間

note のアルゴリズムは投稿頻度だけでなくスキ率・読了率を見る。読まれない記事を毎日出すとアカウント評価はむしろ下がる。

| 期間 | 頻度 | 目的 |
|---|---|---|
| 第1〜2週 | 無料3本 + 有料1本 | 反応の確認とトークン実測。文体とカテゴリの当たりを見る |
| 第3週〜 | 無料5本 + 有料2本 | 数値が取れてから本番頻度へ |

---

## 14. 未決事項

1. **note エディタの HTML ペースト挙動** — 見出し・リスト・引用は保持される想定だが、ステップ5で実データを貼って確認する。崩れる要素が見つかれば `build-preview.mjs` の変換ルールと `lint.py` の検出項目に追加する
2. **Routines のトークン消費がサブスク枠にどうカウントされるか** — 回数制限（5回/日）とは別にトークン枠も消費するはず。ステップ4の実測で確認する
3. **道具型の有料noteが実際に売れるか** — 実績のないアカウントの道具型は「無料で十分では」と判断されやすい。価格は低めから開始し `revenue-maximizer` で調整する
4. **見出し画像** — `image-prompt` を廃止したため画像はなし。note のデフォルト機能で足りるかステップ5で判断する
5. **カテゴリ固定の見直し周期** — 四半期ごとに `learnings.md` の範囲外テーマを棚卸しし、サブカテゴリの入れ替えを検討する
6. **Routines から Web検索・MCP がどこまで使えるか** — `weekly-research` が必要とする外部サイトへのアクセス可否はステップ2で実測する
7. **X 連携** — API 有料のため投稿文の生成までとし、投稿は手動
8. **`dedup.py` の閾値** — 検証で「言い換えただけの記事」が 0.77 となり warn 止まりだった。実データが20本ほど貯まった時点で、ブロック閾値を 0.85 → 0.80 に引き下げるか判断する
9. **Python の依存** — 標準ライブラリのみで実装したため `requirements.txt` は不要。Actions は `pip install` なしで走る。将来 sudachipy が必要になるのは、送り仮名の不統一や同義語の混在を見るときだけである
9. **ダッシュボードの合言葉** — public repo の Pages で配信するため、**合言葉が唯一の防御になる**。15文字以上のフレーズにし、`secrets/passphrase.txt`（`.gitignore` 対象）で管理する。暗号化されているのは数値だけで記事本文は含まないが、売上データが漏れれば実害はある

---

## 付録A: v2.0 スキルの行き先

| v2.0 | v3.0 |
|---|---|
| R1 `daily-trend-scan` | → `weekly-research` に統合 |
| R2 `pain-mining` | → `weekly-research` に統合 |
| R3 `competitor-teardown` | → `weekly-research` に統合 |
| R4 `theme-scoring` | → `weekly-research` に統合（`researcher` が兼務） |
| R5 `persona-design` | → `account-design` に統合（アカウント単位で固定） |
| R6 `product-concept` | 維持（`content-outline` を吸収） |
| R7 `pricing-strategy` | 維持 |
| E1 `content-outline` | → `product-concept` に統合 |
| E2 `sales-letter` | 維持（`productType` 分岐を追加） |
| E3 `draft-writing` | 維持 |
| E4 `letter-audit` | 維持（型分岐、上限2周） |
| E5 `reader-feedback` | 維持 |
| E6 `ethics-line` | 維持（light モードを追加） |
| E7 `proofreading` | → `scripts/lint.py` |
| E8 `note-layout` | → `scripts/lint.py` |
| E9 `title-design` | 維持（15案→8案） |
| E10 `image-prompt` | **廃止** |
| E11 `note-draft-push` | **廃止**（HTML コピペへ） |
| E12 `build-report` | 維持（簡素化） |
| S1〜S5 営業部 | 凍結 |
| C1 `profile-setup` | **廃止**（`account-research` + `account-design` + `profile-accumulator` へ） |
| C2 `revenue-maximizer` | 維持 |
| （新規） | `account-research` / `account-design` / `daily-article` / `profile-accumulator` |

## 付録B: 参考資料との対応

| 出典 | 反映先 |
|---|---|
| ひとり出版社の記事 | Routines / Skills / Subagents の配置。ただし3部署モデルは二層モデルに置き換えた |
| セールスレターの記事 | `sales-letter` の構造、`letter-audit` の判定項目、`ethics-line` |
| 22スキル構成の記事 | スキル分類の思想 |
| プロンプト集11本 | 各スキルの中核ロジック |

**変数の自動充填は v3.0 でも必須要件。** プロンプト集は「毎回人間が `[ここに記入]` を埋める」前提で作られている。完全自動化では人間が埋めないので、各変数がどのファイルのどの項目から引かれるかを、スキル実装時に必ず定義する。ここが埋まらないスキルは自動フローに組み込めない。
