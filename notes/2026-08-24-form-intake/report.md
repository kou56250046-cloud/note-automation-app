# 生成ログ — 2026-08-24-form-intake

日付: 2026-08-24（生成日）/ 公開予定 2026-08-24
種別: 無料記事
連載: 2026-W35B-survey-digest（1つの自動化システムを7分割した月曜・設計）
タイトル: Googleフォームの自由記述を、Geminiが読める形でシートに集める入口設計
文字数: 2598 字（コードブロックを除く）

---

## テーマ選定

引いたテーマ: Googleフォームの自由記述を、Geminiが読める形でシートに集める入口設計（themes.md の id: 2026-W35B-01）

スコア: 強み5 / 市場性4 / 支払い意欲2 / artifact5 = 16/20

選定理由:
strength5: account.md のペルソナ「GASで自動化を組んだが人に任せられない人」の入口そのもの。
installable trigger（onFormSubmit）とシート構造の設計は手元の材料だけで書ける。
market4: market/2026-08-24.md の「GAS」は需要207.0 / 供給32.9件/日で比6.301（有料率11%）。
「GoogleAppsScript」は需要90.0 / 供給20.4件/日で比4.407（有料率10%）。両方とも埋もれない帯。
willingness2: 無料記事想定。設計図だけでは行動が完結せず、次の記事（中核処理）が要る前提。
artifact5: onFormSubmitの設定手順＋シート構造の設計図（回答ID/生テキスト/処理状態列）＋
入口の受信コード全文を出せる。
次点は「経費精算レシートの受付フォーム設計」だったが、経費という題材はethics-line観点で
リスクが増えるため、この系統自体を選ばなかった（ヘッダー参照）。

今日これを選んだ理由: weekly-research が day: mon を割り当て済み。account.md の連載構成表どおりの配置。

**生成日が規定とずれている。** weekly-build は日曜実行を前提にしているが、実際の生成は
2026-08-24（月曜）に行った。market データは research/market/2026-08-24.md を当日取得
しているため、判断材料は最新である。

---

## ゲートの結果

| ゲート | 結果 | 差し戻し |
|---|---|---|
| lint.py | エラー0 / 警告2 | — |
| dedup.py | 最大類似度 0.24（2026-08-28-trigger-slack） | — |
| reader-feedback | pass: True / drop 0件 / note 1件（1周） | 1周 |
| ethics-line（light） | **起動していない**（lint.py の needsAudit が false） | — |

### reader-feedback

- **F5**（L62-196）severity: note — 「いちばん伝えたい／渡したい」という同趣旨の強調が2箇所に出ており、同じ主張を2度押している。本文も2730字とスマホで読むには長め
  → 修正: 強調文はどちらか1箇所に絞る。62行を残すなら196行は「この対比が、その理由です」程度に落とす

### ethics-line — 削除・修正した内容（全件）

**起動していない。** `lint.py` の `needsAudit` が `false` だったため（CLAUDE.md ルール6）。

`lint.py` は E5（断定表現）を `voice.md` の `ng` で、E3（裏付けのない実績）を
`unverified-claim`（金額・割合・人数・期間・フォロワー数）で一次判定する。
どちらも検出がなかったので `auditor` を呼んでいない。

**この設計で漏れるもの:** 数値を伴わない婉曲な実績主張は機械では拾えない。
週1本の reader-feedback と月次レビューで検出する想定である。

### 人間に回した項目（severity: review）

- なし

---

## 書けなかったこと

`profile.md` に根拠がなく、書くのを諦めた主張。

- 執筆エージェントからの報告では該当なし。この記事は手順・設計・コードで構成されており、
  成果の数値主張を必要としなかった
- `profile.md` の実績（記事33本 / フォロワー94人 / スキ中央値8）は旧ジャンル
  （日常・脳科学）の数値であり、AI技術ジャンルの記事では引用していない

---

## 上流への申し送り

| 宛先 | 内容 |
|---|---|
| `weekly-research` | https://developers.google.com/apps-script/guides/triggers/installable を含む sources 1件で足りた |
| `account-design` | サブカテゴリ③の既定タグが `NotebookLM` 固定になっている。材料が Gemini API 側だけの週では使えず、今週は在庫側で `GeminiAPI` に差し替えた。account.md 246行目〜の表の見直しを推奨する |

---

## 機械判定の詳細

### lint.py（エラー0 / 警告2件・ブロックなし）

- L136 [heading-interval] 前の見出しから 153 文字（目安 300）。見出しが多すぎる
- L212 [heading-interval] 前の見出しから 236 文字（目安 300）。見出しが多すぎる

### dedup.py

- 最大類似度 **0.24**（比較相手: 2026-08-28-trigger-slack）
- ブロック閾値 0.85 / 警告閾値 0.70 のいずれも下回る
- **注意:** `dedup.py` の人間向け出力は、警告閾値 0.70 未満のマッチを `matches` に
  入れない実装のため「最大類似度 0.00」と表示される（`scripts/dedup.py` 186行目・195行目）。
  上の数値は同スクリプトの関数を直接呼んで算出した実値である。判定（ブロック0件）は正しい

### 同じ週の他記事との類似度（連載のため近くなる）

同一システムを7分割しているため、既出記事より週内の相互類似度のほうが構造的に高くなる。
実測ではいずれも 0.10〜0.25 で、警告閾値 0.70 を大きく下回った。
