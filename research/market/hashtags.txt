; note_market.py が測るハッシュタグ。1行1タグ。`;` 以降はコメント。
;
; タグ名の先頭に # を付けても付けなくてもよい（読み込み時に外す）。
; コメント記号が `#` ではなく `;` なのは、タグ名自体が # を含みうるためである。
;
; 1タグにつき2リクエスト（人気順＋新着）。self の取得に3リクエスト使うので、
; 上限50リクエストなら23タグまで入る。増やしすぎると途中で打ち切られる。
;
; account.md のサブカテゴリに対応させる。カテゴリを変えたらここも変える。
; 存在しないタグは0件になり、レポートの「取得できなかったもの」に出る。
;
; 2026-08-21 第3版（自動化）に合わせて改訂。
;   Canva を外した（0.559。第3版では扱わない）
;   スプレッドシート / GoogleWorkspace / GeminiAPI / 自動化 を追加した
;   「自動化」は主軸タグにしないが、埋もれ具合を毎週測るために計測はする

; --- サブ① 見せられる自動化（システムに顔を付ける） ---
UIデザイン
LP制作
Webデザイン

; --- サブ② 自動化フローの設計思想 ---
GAS
GoogleAppsScript
スプレッドシート
GoogleWorkspace
業務効率化

; --- サブ③ 差し替えて使うテンプレート ---
NotebookLM
Gemini
GeminiAPI
プロンプト

; --- 主軸にしないが、埋もれ具合を測る ---
自動化
AIエージェント
ClaudeCode

; --- 基準線（市場全体の水準を知るため） ---
生成AI
AI活用
