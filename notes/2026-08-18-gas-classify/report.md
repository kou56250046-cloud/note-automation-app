# 生成ログ — 2026-08-18-gas-classify

日付: 2026-08-17 生成 / 公開予定 2026-08-18（火・曜日の割り当てどおり）
種別: 無料記事
タイトル: 問い合わせをGeminiで自動仕分けする。ラベルを固定するGASの全文
カテゴリ: 生成AIで見せられる成果物を作る / Gemini+GASの自動化

---

## テーマ選定

引いたテーマ: `research/themes.md` の `2026-W34-02`（day: tue）

スコア: 強み4 / 市場性5 / 支払い意欲4 / artifact5 = **18/20**（今週の在庫で最高スコア）

選定理由:
- market5: `market/2026-08-09.md` の実測で GoogleAppsScript は比 **7.854（14タグ中1位）**、有料率14%。
  `market/2026-08-17.md` でも GAS 5.935 / GoogleAppsScript 4.815 で上位を維持している。
  **書く人が少なく、かつ金が動いている領域**である
- artifact5: GASコード全文、分類プロンプト全文、動作条件をすべて出せる

次点1: 「Gmail を Gemini で自動仕分けしてラベルを付ける」— 同じ骨格だが受信箱を触るため、
読者が試すときの心理的な壁が高い。まずスプレッドシートの列を埋めるだけの形にした。**次週の候補として残す**
次点2: 「Gemini とスプレッドシートの連携方法まとめ」— 3つのアプローチの比較記事になり、
手元に残るコードが消えるため落とした

---

## 実物

- GAS コード全文（`classifyInquiries` / `classifyOne` / `parseResult` / `buildPrompt`）
- 分類プロンプト全文
- `CATEGORIES` 配列を先頭に定義し、読者が自分のカテゴリに差し替えられる形にした
- APIキーは `PropertiesService` に置き、ハードコードしない書き方をそのまま見せた
- レスポンスが JSON で返らなかったときのフォールバック（未分類として残す）まで含めた

---

## ゲートの結果

- `lint.py`: **エラー0件 / 警告0件**（2866字 / コードブロック2）
- `dedup.py`: 既出14本と比較、最大類似度 **0.00**。重複なし
- `ethics-line`: **未起動**（`lint.py` の `needsAudit: false`。CLAUDE.md ルール6の設計どおり）
- `reader-feedback`: 対象外（週1本の枠は月曜の記事に充てる）

### 差し戻し 1回（char-count）

初版は本文 **2010字** で、`account.md` の投稿方針（2500〜4000字）を下回った。
`heading-interval` も5箇所で警告が出ていた。**failures だけを渡して差し戻した**（全文の書き直しは指示していない）。

writer の対応:
- 「分類は要約と何が違うか」に、分類ミスがエラーにならず数字が合わなくなるという実害を追加
- `CATEGORIES` 配列を先頭に置いた理由と、ラベル粒度の注意を追加
- `parseResult` が「未分類」にフォールバックする判断理由（処理全体を止めない、失敗を隠さない）を追加
- Gemini API の1日あたりのリクエスト上限を動作条件に追加
- つまずきどころに2項目（権限の承認画面 / 既存ラベルがある行のスキップ仕様）を追加
- 「コード全文」と「分類プロンプト全文」の見出しを `## コードとプロンプトの全文` に統合

**コードは1行も増やしていない。** 増やしたのは本文だけである。

修正後: 2866字 / 警告1件（`normalize` 「既に」→「すでに」）→ 親側で機械的に置換して0件。

---

## 差別化（dedup 対策）

既存の `2026-08-11-gas-minutes`（議事録要約）と「GAS から Gemini を呼ぶ」骨格が同じ。

- あちらは**出力が自由文**、こちらは**決められたラベルのどれか**
- 記事の主題を「選択肢を固定して、想定外の答えを返させない書き方」に置いた
- APIキーの取得手順・トリガーの設定手順は繰り返さず、既存記事に委ねた

結果、類似度 0.00。

---

## 書けなかったこと

`profile.md` に根拠がないため、「時間が削減できた」「精度が上がった」といった成果の主張は書いていない。
動作条件と設計理由の説明にとどめた。

---

## weekly-research への申し送り

- `sources` 4件はいずれも一般的な GAS×Gemini 連携の解説記事で、
  **「分類先ラベルを固定して JSON で強制する」という設計に踏み込んだ一次情報が無かった。**
  次に同系統のテーマを仕込むときは、Gemini API の `responseSchema`（構造化出力の型指定）を
  扱った一次情報を探索候補に加えること
- 土曜の `2026-08-22-gas-resume`（分割実行）から本記事へ逆リンクすると読者導線がつながる
