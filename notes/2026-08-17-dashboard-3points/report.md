# 生成ログ — 2026-08-17-dashboard-3points

日付: 2026-08-17（月・曜日の割り当てどおり）
種別: 無料記事
タイトル: AIが出したダッシュボードを、色数・目盛り・並びの3箇所で読める形にする
カテゴリ: 生成AIで見せられる成果物を作る / AI×Webデザイン

---

## テーマ選定

引いたテーマ: `research/themes.md` の `2026-W34-01`（day: mon）

スコア: 強み5 / 市場性4 / 支払い意欲3 / artifact5 = **17/20**

選定理由:
- strength5: ペルソナ「作れたが画面がテンプレ丸出しで見せられない人」に直撃する。
  AI に管理画面を作らせるとグラフだけは必ず付いてくるが、系列ごとに違う色が割り当てられ、
  目盛りが自動で0始まりでなくなる
- market4: `market/2026-08-17.md` の実測で UIデザインは需要84.5 / 供給11.6件/日、比 **7.255**（14タグ中2位）。
  ただし Webデザインが 08-07 の 7.801 から **0.998 まで後退**しており、サブ①全体の空きが縮んだため5にしなかった
- artifact5: before/after のデモ、変更した CSS/SVG、AI への修正指示の全文をすべて出せる

次点1: 「BIツールのダッシュボード設計7原則」— 市場性はあるが読者の手元が変わらず artifact が2まで落ちる
次点2: 「カラーユニバーサルデザイン」— 主題が配色理論そのものになり、
account.md の「新概念の解説だけの記事を書かない」に寄る。本記事内で根拠として引用するにとどめた

---

## 実物

- デモ: https://kou56250046-cloud.github.io/note-automation-app/demo/2026-08-17-dashboard-3points/
- `demo/before.html` / `demo/after.html` — **外部ライブラリ・CDN・外部フォント・外部画像を不使用。**
  グラフは素の SVG で自己完結させている
- 本文にコードブロック5個（変更した CSS/SVG の差分、AI への修正指示の全文）

---

## ゲートの結果

- `lint.py`: **エラー0件 / 警告3件**（2769字 / コードブロック5）
- `dedup.py`: 既出14本と比較、最大類似度 **0.00**。重複なし
- `ethics-line`（auditor / light）: **clear: true / block 0件**
- `reader-feedback`（critic）: **差し戻し1回**（drop 2件 / note 1件）→ 修正後に確定

### reader-feedback の指摘と対応（全件）

**F3 情報の新しさ（drop）— 旧 L47〜135**

> 手1〜手3の約1000字が、AIと無関係な一般的なグラフ設計の定石である。
> 「軸は0から」「系列が1つなら1色」「重要な数値は左上」の3つは検索の1ページ目に出る。
> AI固有の内容は1行しかなく、しかも根拠がない。

→ **対応済み。** 原則の解説を短く畳み、その分を実物の記述に振り替えた。
各手について「投げた指示のどの語が効いたか」「生成物のどの属性が書き換わったか」を、
コードの該当箇所を指しながら書く形にした（`fill` 属性のみ / 軸ラベル4つと rect の y・height /
`grid-template-areas` の並び順と3つのプロパティ）。

出典をぼかしていた「多くのグラフ描画ライブラリは〜」の一文は、
**名指しできないため削除した**（ethics-line の severity: review と同じ箇所）。

**⚠️ critic は「指示が通らなかった回数を書け」と提案したが、採用していない。**
`profile.md` に根拠のない数値・実績は書けない（CLAUDE.md ルール3）。
書いたのは記事に載っている指示文と生成物そのものの記述だけである。

**F5 長さと密度（drop）— 旧 L24〜34**

> L24-32 が手2を、L34 が手1を先取りして説明し切っている。
> 冒頭で結論を出したあと手1到達まで約780字あり、その大半が後続の先取りである。
> 終盤も「軸のラベル、色の数、配置」の順序が4回繰り返されている。

→ **対応済み。** 先取り部分を削除して作例提示へ直結させた。終盤の反復も1箇所に集約した。

**F6 出口（note・記録のみ）**

> 今日やることが3つある。3つ置くと1つも実行されない。
> かつ3つとも「確認」で終わり、コピペできるものがない。
> 指示文は「直近の月(6月)」など作例固有の記述を含み、読者の画面に貼れない。

→ **対応済み。** 「今日できること」を1つ（軸ラベルの一番下の数字を見る）に絞り、
作例固有の記述を含まない汎用の指示文を1つ置いた。

**通過**: F1 F2 F4

### 差し戻し後の再検査

- `lint.py`: エラー0件（2542字 / コードブロック6）。
  差し戻し直後は L99 が101文字で **error（上限100）** だったため、親側で2文に分割した。
  同時に本文が2493字で下限を割っていたが、分割で足りた
- `dedup.py`: 重複なし

### lint.py の警告（ブロックされなかったものも記録する）

- L40 `sentence-length` 一文が90文字（目安60）— デモURLを含む行。URL自体が長いため許容した
- L86 `unverified-claim` 金額の主張（`100万円`）— **ethics-line を起動する条件に該当。起動した**
- L173 `heading-interval` 前の見出しから142文字（目安300）— 「つまずきどころ」節。許容した

### ethics-line の判定（全件）

**起動理由**: `lint.py` の `needsAudit: true`（L86 の金額表現）

**E3（裏付けのない実績）: 検出0件**

L86 の「100万円」「145万円」は**デモ画面の作例ダミー数値**であり、書き手の実績主張ではないと判定された。
`demo/before.html` の軸設定（90〜150 を 200px に対応）と棒の高さが本文の記述と整合しており、
数値カード `¥1,450,000` とも一致する。before / after の両方に同じ値が入っている。

**E5（断定表現）: 検出0件**。`voice.md` の `ng` 29語を本文全体に照合して該当なし。

**削除・修正した箇所: なし**（auditor は本文を書き換えていない）

### severity: review として人間に回した項目（2件）

**1. L36 生成物の由来** — `demo/before.html` に
「目盛りが90から始まっている。差が実際より誇張される」のような**自己批判的な日本語コメント**が
埋まっている。AI の出力そのままにこの種のコメントが入るのは考えにくく、記事用の加筆の可能性がある、という指摘。

→ **対応済み。** 本文を次のとおり修正した。
- 修正前: 「左がAIの出力そのまま、右が直したものです」
- 修正後: 「左はAIの出力によくある形をそのまま残したもの、右が直したものです」
  ＋「左のHTMLには、どこが問題なのかを示す説明コメントだけをこちらで足してあります」の1文を追加

**2. L28 母数のない一般化** — 「多くのグラフ描画ライブラリは〜既定にしています」に出典がない。
書き手の実績主張ではなく第三者ソフトウェアの既定挙動の記述のため block ではない、という指摘。

→ **対応済み。** 「グラフ描画ライブラリには、〜しているものがあります」に弱めた。

なお light モードのため **E1・E2・E4 は検査していない。** `clear: true` はその3項目を検査した結果ではない。

---

## 差し戻し回数

- `writer` への差し戻し: **0回**
- 親側での機械的修正: 2箇所（上記 severity: review への対応）

---

## 書けなかったこと

`profile.md` に根拠がないため、以下は書いていない。

- 「この直し方で問い合わせが減った」等の効果測定
- 読みやすさが向上したという第三者の反応

---

## weekly-research への申し送り

- `sources` 4件で手順の裏付けは足りていた
- ただし「グラフ描画ライブラリが最小値付近から軸を始める既定動作を持つ」という一般論について、
  **一次情報の出典が1本もない。** ethics-line でも母数のない一般化として指摘された。
  次に同系統のテーマを仕込むときは、ライブラリの公式ドキュメントを `sources` に入れること
